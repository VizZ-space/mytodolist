# 易用性评审 · 我的任务台（2026-09-15）

评审方式：跑 `.smoke/shot.mjs` 出 18 张真实界面截图逐张人工核对，再回到 `src/index.html` 逐条找代码证据（标注 `文件:行`）。
不是凭印象列感想 —— 每条都指到具体实现，且末尾单列「怀疑过但核实无问题」的部分。

结论：功能完整度已经很高，**剩下的问题集中在"入口可见性""同一件事两种做法""破坏性操作的分量"三类**，都属于"不改功能、只改呈现"的低风险优化。

---

## 一、优先级速览

| # | 问题 | 优先级 | 改动量 |
|---|---|---|---|
| 1 | 设置面板顶部没有关闭入口，底部关闭按钮会随内容滚走 | **P0** | 小 |
| 2 | 「今天要处理 N 项」的 N 与列表行数对不上（4 行 3 项） | **P0** | 小 |
| 3 | 设置里的备份提醒是"死胡同"—— 叫你导出却不给按钮 | **P0** | 小 |
| 4 | 搜索框提示 `⌘F` 但按下去弹的是另一个界面；`⌘K` 快速录入零入口 | **P0** | 中 |
| 5 | KPI 四格是"包含关系"却做成"并列"，数字加总不等于任务数 | P1 | 中 |
| 6 | 列表页与看板页的筛选条不是一套（看板多"项目"筛选与维度标签） | P1 | 小 |
| 7 | 卡片右下「去拆解」和「展开」是同一个按钮的两种叫法 | P1 | 小 |
| 8 | 「删除这个任务」做成整行大红按钮，紧贴「保存」下方 | P1 | 小 |
| 9 | 点遮罩关弹窗的行为不一致，且静默丢半填内容 | P1 | 中 |
| 10 | 设置面板两列高度严重失衡、分组层级平、关闭按钮用主色 | P1 | 中 |
| 11 | 列表/今天页没有行内快速添加（加一条任务要 4 步） | P2 | 中 |
| 12 | 没有快捷键一览 | P2 | 小 |
| 13 | 空态只"指路"不给按钮，文案还折行 | P2 | 小 |
| 14 | 项目行是纯图标按钮、客户行带文字，同一个弹窗里两种写法 | P2 | 小 |
| 15 | 侧栏 6 个分组偏长，0 计数的标签也占位 | P2 | 中 |
| 16 | 任务卡整卡都是"打开编辑"的热区，误触率高 | P2 | 小 |

---

## 二、P0：会让人卡住或丢东西

### 1. 设置面板顶部没有关闭入口

**现象**：打开设置后，顶部看不到任何关闭方式；「关闭」按钮在面板最底部，而面板内容比一屏高。

**证据**：
- `.sheet{max-height:88dvh;overflow:auto}`（`index.html:444`）——整个面板是一个滚动容器，页头页脚都在里面
- `.set-foot{...margin-top:18px;padding-top:16px;border-top}`（`index.html:432`）——**没有 `position:sticky`**，会随内容滚走
- `openSettings()` 的结构是 `set-head + bkinfo + set-grid + set-foot`（`index.html:3925-3968`），关闭按钮只出现在 `set-foot` 里（`3966`）
- 18 个弹窗**没有任何一个**有右上角 ×（grep `sheet-close` / `xbtn` 无结果）

**建议**：
1. `.set-foot{position:sticky;bottom:0;background:var(--sheet);}`（加个上边框已经现成）
2. 给 `.sheet` 统一加一个右上角 ×。注意 `.set-head` 已经是 `display:flex;justify-content:space-between`（`425`）但**只放了一个子元素**——这个槽位本来就是给关闭按钮留的，加上去是 1 行改动

---

### 2. 「今天要处理 3 项」下面有 4 行

**现象**：标题写 3 项，列表却有 4 行，第 4 行只是文字变浅，没有小标题或分隔，会让人怀疑算错了。

**证据**：
```js
// index.html:2627  计数只取 list
'<div class="today-h">' + icon(IC.sun,17) + "今天要处理" +
  (list.length ? '<span class="cnt">' + list.length + " 项</span>" : "") + '</div>'

// index.html:2639-2645  渲染却是 list + soon 两组
list.forEach(...)   // 今天到期 + 逾期        → 3 条
soon.forEach(...)   // 3 天内到期（仅 muted） → 1 条
```
`list` 的取值是 `dayDiff<=0`，`soon` 是 `0<dayDiff<=3`（`2622-2623`）。所以 3 + 1 = 4 行而标题只报了 3。

**建议**：给 `soon` 补一行分组小标题再渲染，沿用已有的 `.sec-h` 模式：

```
今天要处理 3 项
  ▸ 整理 Q3 需求评审材料   逾期 2 天
  ▸ 回复客户 A 的报价单     今天截止
  ▸ 确认合同附件条款        逾期 5 天
接下来 3 天 · 1 项            ← 新增
  ▸ 每周复盘：本周踩了哪些坑  2 天后到期
```
（另一种最小改法：标题计数改成 `list.length + soon.length`，但分组更清楚，也更符合"今天"这个词的边界。）

---

### 3. 设置里的备份提醒是死胡同

**现象**：橙色条写着"还没有导出过备份 / 建议现在导出一份，换设备或误操作时能立刻恢复"，但**条里没有导出按钮**，而导出/导入挂在顶栏 —— 用户读完这句话得自己去找入口。

**证据**：`index.html:3927`，`bkinfo` 只拼了 `icon + <b>txt</b> + <span>sub</span>`，没有任何 button；`backupInfo()` 也只返回文案。

**建议**：条内直接给一个「立即导出」按钮，复用已有的 `data-act="export"`。这里是全应用**唯一**主动劝用户备份的地方，最该有按钮的就是它。

> 顺带回答之前挂起的那个问题：导出/导入确实更应该待在设置里（`数据存储位置` 区块下方），顶栏那排留给"搜索 / 新建"就够了。加上这一条，设置里的数据安全叙事才闭环。

---

### 4. 搜索结果与快捷键提示不一致，`⌘K` 完全没有入口

**现象**：
- 顶栏搜索框的 placeholder 是「搜索任务 ⌘F」
- 在框里打字 → 就地过滤列表（`index.html:1837` 的 `oninput`）
- 按 `⌘F`（或 `⌘K`）→ 打开的是**另一个界面**：命令面板（`index.html:4990-4991` 都调 `openPalette()`）

于是同一个提示词对应两种完全不同的行为。而命令面板其实是本应用最有效率的功能（自然语言录入 `!p1 @标签 明天`、`*` 标重要、直达任意视图），**界面上零入口**，不翻文档发现不了。

**建议**：
1. 让 `⌘F` 去聚焦顶栏那个输入框（符合所有软件的习惯），命令面板只留 `⌘K`
2. 顶栏或侧栏给命令面板一个可见入口，例如搜索框左侧加一个「⚡ 快速录入 ⌘K」按钮，或在"今天"页空态里点一句

---

## 三、P1：一致性与认知负担

### 5. KPI 四格把"包含关系"画成了"并列关系"

**证据**（`index.html:1865-1884`）：
```js
if(t.done){done++;return}          // 已完成：与待办互斥
todo++;                            // 待办：所有未完成
if(t.due && dayDiff(t.due)<0) od++;        // 逾期 ⊂ 待办
if(t.status==="doing"||有子任务已完成) doing++;  // 进行中 ⊂ 待办
```
示例数据实测：待办 4 / 逾期 2 / 进行中 1 / 已完成 2。数字本身是自洽的（待办 4 + 已完成 2 = 侧栏「任务 6」），但**四张等宽卡片并排会让用户读成 4+2+1+2=9**，而侧栏写着 6。

**建议**：把"待办 / 已完成"作为一对总量，"逾期 / 进行中"降级为待办卡内的小字副标题：

```
┌─ 待办 4 ────────┐  ┌─ 已完成 2 ─┐
│ 其中 逾期 2 · 进行中 1 │  └───────────┘
└─────────────────┘
```
（KPI 卡本身是可点的钻取入口，这一点做得好，保留。）

### 6. 列表页与看板页的筛选条不是一套

| | 列表页 `filterBar()` `2578` | 看板页 `boardFilterBar()` `2850` |
|---|---|---|
| 侧栏维度芯片 | ✓ | ✓ |
| 项目下拉 | ✗ | ✓ |
| 维度标签前缀 | ✗ | ✓（`项目` / `优先级`） |
| 优先级芯片 | ✓（裸 `P 全部` `P 高`） | ✓（带 `优先级` 标签） |

同一组筛选，切到看板会多出东西、还会换一种排法。另外 `P 全部 / P 高 / P 中 / P 低` 这个前缀写法可读性差，看板里同一个东西明明写的是"优先级"。

**建议**：抽一个公共筛选条，默认都带"项目 + 优先级 + 侧栏维度芯片 + 清空筛选"，前缀统一用「优先级」而不是「P」。

### 7. 「去拆解」和「展开」是同一个按钮

```js
// index.html:2912  有子任务
'<button class="exp" data-act="expand">' + (open?"收起":"展开") + ...
// index.html:2914  没子任务
'<button class="exp" data-act="expand">' + (open?"收起":"去拆解") + ...
```
两者都是 `data-act="expand"`，行为完全一样（就地展开出"＋添加子任务"输入框）。但"去拆解"暗示要跳转到别的页面，与"展开"不是一种语气，同一位置两个名字会让用户以为点了会去不同地方。

**建议**：统一成"展开 / 收起"；想强调可拆解就用图标或占位文案（"还没拆子任务"已经承担了这个提示）。

### 8. 「删除这个任务」的分量过重、位置过近

**证据**：`index.html:3706` 与 `3872`，删除按钮是 `class="btn-danger btn-wide"`（整行），紧跟在「保存」下方（`sheet-act` 距它只有 `margin-top:18px`）。

**问题**：破坏性操作拿到了和主操作同等的视觉权重，且就贴在主操作正下方 —— 鼠标从"保存"往下滑一点就命中。第 8 轮刚统一了危险色规范（这点是对的），但**"是红的"不等于"该是整行大按钮"**。成熟的同类产品（Things 3、Todoist）都把删除放次要位置。

**建议**：改成弹窗左下角的文字链（红色小字），或收进「更多选项」里。保留二次确认框兜底。

### 9. 点遮罩关弹窗：行为不一致，还静默丢内容

**证据**：
```js
// index.html:4505
if(a==="maskClose"){ if(ui.editing && ui.editing!=="__new__")return; closeModal();return }
```
- **编辑已有任务**时点遮罩 → 直接 `return`，不关（防误关，意图是好的）—— 但**没有任何反馈**，用户会以为界面卡了
- **新建任务 / 客户 / 项目 / 标签表单**点遮罩 → 立即关闭，**半填内容静默丢失**

现在是"该保护的没保护，保护了的又不说"。

**建议**：统一成一条规则 —— 表单有改动时点遮罩，先弹一句"内容还没保存，确定丢弃吗？"；没改动就直接关。不要静默吞掉点击。

### 10. 设置面板的版式与层级

从 `settings-modal.png` / `settings-footer.png` 看：

1. **两列高度严重失衡**：左列（AI 总结）一路排到快照，右列 5 个区块结束后留了一大片空白
2. **分组层级平**：右列「数据存储位置 / 自动快照」属于"数据"，「登录时启动 / 到期提醒 / 新建任务快捷键」属于"系统"，现在等权平铺，没有大类标题
3. **「关闭」用了主按钮蓝**（`index.html:3966` 的 `btn-pri`）——但设置是**即时生效**的，蓝色主按钮会让人以为它承担"保存/应用"
4. **「6 个任务 / 3 个项目」夹在「清空全部数据」和「关闭」之间**（`3965`），位置莫名，属于噪声

**建议**：加两个大类标题（「数据」「系统与通知」）把右列分组；`关闭` 降为 `btn-ghost`；统计信息挪到页头标题下方或直接去掉。

---

## 四、P2：效率与习惯

### 11. 没有行内快速添加

加一条任务的路径是：`新建` → 弹窗 → 填标题 → `保存`（4 步）。而子任务反而有现成的行内输入："＋添加子任务，回车确认"（`index.html:2917` 起的 `.subs` 区）。

grep `qadd / quickAdd / addTask` 均无结果 —— 顶层任务没有等价的入口。

**建议**：在"今天"和"任务"列表顶部放一个和子任务同款的行内输入（回车即建、支持 `!p1 @标签 明天` 语法）。这能把最高频的动作从 4 步压到 1 步。

### 12. 没有快捷键一览

设置里只有「新建任务快捷键」一块（`3957`）。实际散落着：`⌘K` 命令面板、`⌘F` 搜索、`⌘,` 设置、`Esc` 逐层退、卡片拖拽换状态、子任务拖拽排序。

**建议**：设置里加一块只读的「快捷键」表，或在命令面板里加一条"查看快捷键"。

### 13. 空态只"指路"不给按钮

侧栏空态文案是「还没有项目，点标题右侧的 + 新建」（`1736-1752` + `emptyState`），而且因为列宽窄，**"新建"两个字被折行成了"新 / 建"**（见 `today-empty.png`）。

对还没建立心智模型的新用户，"请去点别处那个图标"是额外成本。注意：**这不是第 9 轮删掉的那种重复入口** —— 空态里的按钮只在"一个都没有"时出现，和常驻的 `＋` 不冲突。

**建议**：空态直接用 `acts:[{label:"新建项目", act:"newProject"}]`（代码里已有这个能力，见 `3554` 行的项目页空态就是这么做的），文案改成"项目用来把一堆相关任务归到一起"这类解释。

### 14. 同一个弹窗里，项目行和客户行两种写法

```js
// index.html:3679  项目行：两个纯图标按钮
'<button class="tbtn" data-act="newProjectInline" title="新建项目">'+icon(IC.plus,14)+'</button>'
'<button class="tbtn" data-act="editProjectInline" title="编辑当前项目">'+icon(IC.pencil,14)+'</button>'

// index.html:3683  客户行：同样的东西带文字
'<button class="tbtn" data-act="newClientInline" title="新建客户">'+icon(IC.plus,14)+' 新建客户</button>'
```
（`title` 是有的，所以悬停能看到说明；但图标按钮在触屏上等于没有标签。）

**建议**：统一 —— 要么都带文字，要么都纯图标。同一个弹窗里两行长得不一样，用户会以为它们的性质不同。

### 15. 侧栏偏长

分组有 6 组：常用 / 更多视图 / 项目 / 客户 / 标签 / 智能列表（+ 底部钉住的设置）。到"标签"那一屏已经要滚动了（见 `sidebar-scrolled.png`）。且 0 计数的标签也占位（如"市场推广 0"）。

另外「常用」里的"项目"导航项与下方的"项目"分组同名不同义（一个是页面入口，一个是项目列表），容易被当成重复。

**建议**：标签 / 智能列表默认折叠（点标题展开）；0 计数的标签隐藏或弱化；把「常用」里的项目项改名为"项目总览"以区分。

### 16. 任务卡整卡都是热区

```js
// index.html:2863
'<div class="task" data-act="edit" data-id="...">'
```
整张卡是打开编辑弹窗的按钮，卡内又嵌了 checkbox、标签芯片、重要星标、"⋯"菜单、下拉、甚至行内输入框。大面积热区套小热区，误触概率不低（想点标签、想选文字都会开弹窗）。

**说明**：这是第 11 轮按你的要求做的（点卡片直接开编辑弹窗），属于**有意的取舍**，我没打算推翻。如果想降低误触，最小改动是把 `data-act="edit"` 从 `.task` 挪到标题那一行（`.ttl`），卡片其余部分保持无动作。

---

## 五、怀疑过、但核实没问题的（避免误改）

| 怀疑 | 核实结果 |
|---|---|
| KPI 卡是"只展示不可点"的死卡片 | ❌ 误判。是 `<button data-act="kpi">`，可点击钻取并高亮（`index.html:1875-1880`），0 计数时置灰不可点，做得对 |
| 日期面板里下个月的日期没弱化，会误点 | ❌ 误判。`.dp-cell.out{color:var(--faint);background:var(--bg)}`（`607`），已弱化 |
| 项目行那两个图标按钮没有提示 | ❌ 误判。`title` 属性齐全（`3679`）；真正的问题是**同一弹窗里客户行带文字、项目行不带**（见 P2-14） |
| 删除任务/笔记/项目没有撤销 | ❌ 误判。有 6.5 秒撤销 toast（`toast(msg, undoLabel)`，`1674`；笔记 `4081`、项目 `4217`、任务 `4573`） |
| KPI 数字和侧栏任务数对不上，是算错了 | ❌ 不是 bug。待办 4 + 已完成 2 = 6 = 侧栏数，逾期/进行中是待办的子集。**只是版式没传达包含关系**（见 P1-5） |
| 按钮几何散落在各处 | ❌ 已解决。第 8 轮已下沉到 `.btn-*` 变体 + `--sheet-*` 尺寸阶梯 |

---

## 六、建议的实施顺序

1. **先做 P0 的四条**（1、2、3、4）—— 全是入口可见性/信息一致性的小改动，不碰数据模型，风险最低
2. **再做 P1 里"纯呈现"的三条**（5、6、7）—— 统一口径，用户感知明显
3. **然后 P1 的 8、9**（破坏性操作分量 + 遮罩一致性）—— 涉及交互规则，改完要补冒烟断言
4. **P2 按需**：11（行内快速添加）收益最大但改动中等，15、16 属于打磨

每条改完沿用既有验收：`build:front` → `smoke.mjs` → `audit.mjs` → `modal-audit.mjs` → `shot.mjs` 截图核对。

---

## 七、实施记录（2026-09-15 晚）

用户选定：**1–9 全做，11、15、16 也做**（10、12、13、14 本轮未做）。全部已落地并通过验收。

| # | 改动 | 落点 |
|---|---|---|
| 1 | 21 个弹窗全部注入右上角 `×`（`sheetX()`）；`.set-foot` 改 `position:sticky` 吸底 | `.sheet-x`、`.set-foot` |
| 2 | 「今天要处理」下面的 3 天窗口另起分组标题「接下来 3 天 · N 项」（`SOON_DAYS` 常量） | `viewToday()`、`.today-sub` |
| 3 | 备份提醒条内加「立即导出」按钮 | `.bkinfo-act` |
| 4 | `⌘F` 改回聚焦顶栏搜索框；搜索框内加常驻 `⌘K` 徽标打开命令面板；placeholder 去掉误导提示 | `.kbd-btn`、键盘分发器 |
| 5 | KPI 改父子版式：`[待办 N｜其中 逾期 x·进行中 y] [已完成 z]`，四个钻取按钮统一带 `data-act="kpi"` + `.kpi-n` | `kpiStrip()`、`.kpi-hero/.kpi-sub` |
| 6 | 删 `boardFilterBar()`，列表与看板共用 `filterBar()`；项目筛选统一到 `ui.pid`，删 `ui.bproj`；优先级芯片去掉旗子图标 | `filterBar()`、`boardTasks()` |
| 7 | 「去拆解」统一为「展开」 | `taskCard()` |
| 8 | 5 处表单删除按钮从正文整行 `btn-danger.btn-wide` 降为页脚左侧 `.btn-ghost.btn-del` 文字链；`.btn-wide` 下线；确认框仍用 `btn-danger` | 任务/项目/客户/标签/智能列表五个弹窗 |
| 9 | 遮罩关闭改为"动过就不关 + 抖动"（`_maskDirty` 事件标记 + `.sheet.nudge`），取消 `ui.editing` 特判 | `#modal` 捕获监听、`maskClose` 分支 |
| 11 | 行内快速添加 `#qi`（仅任务页列表形态），继承项目/客户/标签筛选，回车连续录入 | `quickAdd()/quickAddRow()/inlineAddCommit()` |
| 15 | 侧栏「标签」「智能列表」默认折叠（`ui.sideFold`），隐藏 0 计数标签（当前筛选中的保留） | `renderSide()`、`sideHead()`、`.side-fold` |
| 16 | 任务卡热区收窄：根 `.task` 去掉 `data-act` 与 `cursor:pointer`，只有 `.ttl-link` 打开编辑 | `taskCard()`、`.task`/`.ttl-link` |

### 顺带修掉的两个真问题（不在 16 条里）

- **浏览器预览下保存必然失败**：`flushSave()` 直接调 `invoke("save_store")`，非 Tauri 环境（双击打开 `dist/index.html`）会抛错并在顶部弹红色"数据保存失败"横幅——每次启动都能看到，像应用坏了。新增 `storeRead/storeWrite` 通道：桌面走 Rust，浏览器退回 localStorage。Tauri 路径行为不变。
- **`.sheet.nudge` 与新增抖动动画重复**：改用已有的 `.nudge`，不再加第二份 `@keyframes`。

### 验收结果

| 脚本 | 结果 |
|---|---|
| `smoke.mjs` | **44/44**（新增 KPI 口径 2 项、`Q1/Q2` 行内添加 2 项、`S1` 侧栏折叠、`S2` 卡片热区、`M1` 遮罩行为） |
| `modal-audit.mjs` | 21 个弹窗全过（等宽规则为 `.btn-del` 开了例外） |
| `entry-audit.mjs` | 13/13 |
| `audit.mjs` | 死入口 0 / 零引用函数 0 / 零引用变量 0 / 未用图标 0 / 未用 CSS 0 |
| `responsive.mjs` | 7/7 |
| `shot.mjs` | 20 张截图逐张核对 |

### 本轮学到的两条（写进 `ui-spec.md`）

- **布局顺序里"控件在前、生效芯片在后"**：可移除的筛选芯片宽度会变，放在行首会让整排控件左右抖动。
- **同屏数字必须自洽**：分组标题的计数只能算它下面那一段；包含关系要在版式上表达，不能靠用户自己加。

