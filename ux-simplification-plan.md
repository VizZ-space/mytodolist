# 我的任务台 · 交互简化优化方案

> **诊断依据**：对 `my-task-desktop/src/index.html`（4400 行）与 `src-tauri/src/main.rs`（1852 行）的逐项走查，所有结论均落到具体函数与行号。
> **调研对象**：Things 3、Todoist、TickTick、Linear、Apple Reminders、Google Keep
> **日期**：2026-09-11
> **版本**：**v1.2** —— 修订：**交互范式从"高阶用户优先"校正为"大众习惯优先"**。v1.1 中若干设计（用内联语法替代可见控件、hover 显隐、命令面板当主入口、Notion 式选区浮层、双链语法）属于"高阶玩家习惯"，不符合大部分用户的直觉，已逐条改写，并新增第 3.5 节《交互通道规范》。
> **历史**：v1.1 曾把笔记误收进折叠区（已撤销）；v1.0 曾断言"纯前端改动、不碰数据库"（加入改造包 E3 后已修正）。

---

## 0. 一句话结论

这个 App 的问题**不是"流程太长"，而是"同屏选项太多、层级太多、入口语义错位"**。

实测数据显示：从打开 App 到加入第一条任务**只要 3 步**，而且**没有任何必填字段**——技术门槛其实很低。但与此同时，**今天视图同屏有 30+ 个可点控件**，一级导航 **10 项**，任务编辑弹窗 **12 个字段**，首启还直接灌入示例数据。

所以用户感觉到的"复杂"，来源不是"走不完流程"，而是**一眼看不到重点**。这不是功能问题，是**信息架构与优先级问题**。

> **v1.2 补充——第二类问题：交互范式选错了参照系。**
> 代码注释里明确写着"macOS 操作习惯"（L835、L4352），实现也大量采用 **keyboard-first / hover-hidden** 手法：导航靠 `⌘1–8`、新建靠 `⌘N`、行内操作靠悬停、格式靠语法。
> 这些是**熟练用户的加速器**，但对大部分用户来说是**隐形功能**——看不见，就不知道它存在。
> 因此简化必须遵循一个前提：**减少同屏选项，但不能把功能藏起来。** 藏起来的简化是"以牺牲可发现性换来的整洁"，对大多数用户是净损失。

---

## 1. 复杂度体检（实测数据）

| 指标 | 现状 | 健康值 | 判定 |
|---|---:|---:|:--:|
| 一级导航项 | **10** | ≤5 | 超标 2 倍 |
| 导航重复渲染 | **2 套**（左栏 + 底部 tabbar） | 1 | 冗余 |
| 今天视图可点控件 | **≈30+** | ≤12 | 严重超标 |
| 任务编辑弹窗字段 | **12+** | 3 + 折叠 | 超标 4 倍 |
| 新建项目弹窗字段 | **11** | 3 + 折叠 | 超标 |
| 笔记页工具栏按钮 | **18** | ≤6 | 超标 3 倍 |
| 报告页控件 | **25+** | ≤10 | 超标 |
| 快速添加条控件 | **6**（输入 + 3 下拉 + 日期 + 按钮） | 1 输入组 | 超标 |
| 添加任务最短步数 | 3 | ≤3 | ✅ 合格 |
| 必填字段数 | 0 | 0 | ✅ 合格 |
| 统一空状态 | 无（首启灌示例数据） | 每视图 1 个 | 缺失 |
| 首次使用引导 | 无 | 1 个轻量引导 | 缺失 |
| 死入口 / 隐形入口 | 3 处 | 0 | 缺陷 |
| **只能悬停才能发现的操作** | **1 处**（项目卡 `.pcard .h .iact{opacity:0}`，L286） | 0 | 缺陷 |
| **快捷键在 Windows 上失效** | **2 组**（`⌘1–8` 切视图、`⌘\` 开关右栏） | 0 | **bug** |

**结论：步数和必填项已经很好，真正的债在"可见控件数量"和"信息架构"。**

---

## 2. 六个根因

### R1 · 同屏控件过载（最主要）

- `viewToday` 里单个视图的可见控件 ≈30 个：快速添加条 6 + 筛选芯片 4 + 批量治理 2 + 每张任务卡 ≈8 + 完成条
- 笔记视图工具栏 **18 个按钮**平铺（`viewNotes`）
- 报告视图 25+ 控件一次性铺开（`viewReport`）
- **问题**：新用户看到的是"一整面控制面板"，而不是"一张待办清单"

### R2 · 双重导航

- 左栏 `renderSide()` 渲染 10 项视图（`tabs` 数组，L1627）
- 底部 `renderTabbar()` 又渲染**完全相同的 10 项**（L1688）
- 桌面端底栏被 `display:none` 隐藏，也就是说**这 10 项在源码里存在两遍，其中一遍永远不显示**
- 附带 bug：`renderMain()` 的标题映射表（L1702）**只有 9 项、缺「项目」**，导致进入项目视图时标题错误回退为"今天"

### R3 · 视图切换与筛选混排

- 侧栏里「今天/日历/任务/看板…」这些**视图**与「全部任务/未归类/各个项目/客户/标签/智能列表」这些**筛选器**平铺在一起，没有任何视觉分组
- 更糟的是**筛选语义不统一**：`filtered()`（L2202）是全局筛选，但**看板视图自己另走一套** `boardTasks`（L2447）
- **问题**：同一界面里"点一下换个页面"和"点一下筛掉一半内容"长得一模一样

### R4 · 入口语义错位：点「新建」打开的是命令面板

- 顶部那个显眼的「新建」按钮（L1712），点击后执行的是 `openPalette()`（L3901）
- 新用户点"新建"，**看到的是一个搜索框**
- `⌘N`（L4319）同样指向 `openPalette()`
- **问题**：命令面板是**熟练用户的加速器**，现在却承担了主入口职责

### R5 · 没有渐进披露，一次给全

- 任务弹窗 `openTask`（L3252）一次呈现 12+ 字段：标题/客户/项目/优先级/状态/日期/标签/重要/关联笔记/子任务/备注/跟进记录
- 项目弹窗 `openProject`（L3398）11 个字段：预算/合同额/阶段/落地可能性/合同类/签署日/落地年/摘要/目标……
- 弹窗里还能再开弹窗（`newClientInline` → `openClient`，L3267/L3755）
- **问题**：一个个人任务工具，却按照"合同管理系统"的字段密度设计

### R6 · 交互范式是"高阶用户优先"，不是"大众优先"（v1.2 新增）

| 证据 | 位置 | 问题 |
|---|---|---|
| 注释「阶段六：macOS 打磨（完成动效 / **克制 hover** / 对比度）」 | L835 | 整个阶段的主题就是 hover 手法 |
| `.task:hover .acts{opacity:1}` | L839 | 以悬停作为行内操作的可见条件 |
| `.pcard .h .iact{opacity:0}` / `:hover` 才 `opacity:1` | L286–287 | **项目卡操作按钮新用户根本发现不了** |
| 注释「macOS 操作习惯：双击编辑 + 右键菜单」 | L4352 | 隐藏手势 + 右键，均为"不教就不会" |
| `⌘1–8` 切视图、`⌘\` 开关右栏 | L4343–4344 | 快捷键承担导航职责 |
| `⌘K` / `⌘F` 都打开命令面板 | L4346–4347 | 主入口是面板而非可见控件 |
| `!高 !中 !低 @标签 明天` 解析能力塞在 42 字 placeholder 里 | `quickAdd` L2169 | 语法是唯一入口，且不可发现 |

**核心问题**：hover 在触屏上不存在；快捷键不可发现；语法/双链需要记忆。它们本该是**加速器**，现在却承担了**主路径**职责。

---

## 3. 市场调研：别人怎么解决同一个问题

| 产品 | 核心手法 | 可借鉴之处 |
|---|---|---|
| **Things 3** | 刻意不做日历、不做笔记、不做报告。只有 Areas → Projects → Tasks 三层；Today / Upcoming / Anytime 三个视图由日期**自动推断**，用户不需要手动分类 | **"缺少功能也是一种功能"**。视图应该由数据自动推断，而不是让用户手动切换 10 个页面 |
| **Todoist** | Inbox 兜底——**不建任何项目也能完整使用**；自然语言一次输入 `提交报告 周五 2pm @工作 #市场` 自动解析出日期+项目+标签 | **捕获必须零决策**。但注意：其语法解析是**卖点带来的学习成本**，不适合作为大众产品的主路径 |
| **Linear** | 强默认、空状态即引导、行内操作 hover 才出现、⌘K 是主要入口 | 强默认 ✅ 借鉴；**hover 与 ⌘K 属于典型的专业用户范式**，本案只可作**加速**，不可作唯一入口 |
| **Apple Reminders** | 系统级零配置，打开就能写，智能列表是可选进阶 | 个人工具的第一屏应该只有一个输入框 |
| **Google Keep** | **不要求标题、不要求分类**，点开就写，保存即完成 | 降低"启动成本"比降低"操作步骤"更重要 |
| **TickTick（滴答清单）** | 功能很全（日历/番茄/习惯/四象限），但全部靠**左侧二级入口**收纳，主区始终是任务列表；添加任务是**就在列表顶部插一行** | **最贴近中国大众用户的心智模型**，本案的导航与新增交互应以它为基准 |
| **微软 To Do** | 左侧「我的一天 / 重要 / 已计划 / 任务」+ 可自定义清单；新增任务就是**顶部一行输入框** | 导航分组与"就地新增"的直接参照 |

### 提炼出的八条设计原则

1. **捕获优先（Capture First）** —— 第一屏只回答一个问题："这条想法放哪？"其余全部后置
2. **强默认代替提问（Opinionated Defaults）** —— 能用默认值决定的事，就不要让用户选
3. **渐进披露（Progressive Disclosure）** —— 高级功能在"需要的那一刻"出现，而不是一开始摆出来
4. **空状态即引导（Empty States as Onboarding）** —— 每个空视图都要说明"这里放什么"并给一个动作
5. **导航分层** —— 一级只留天天用的 3–5 个；其余**降权但仍在**（不是消失）
6. **操作分层：常驻精简 + 溢出菜单** —— 行内只留「勾选 + 标题 + `⋯`」，其余进 `⋯` 菜单与右键菜单。**不用 hover 做唯一入口**
7. **可见通道优先（Mouse-First）** —— 每个功能都必须有"看得见、点得到"的入口；快捷键与内联语法只做加速
8. **沿用大众熟悉的既有习惯** —— 不发明新交互。左勾选框、点标题打开、`⋯` 菜单、右键菜单、工具栏图标（Word 式）、回车确认、Esc 取消、双击编辑

---

## 3.5 交互通道规范（v1.2 新增 · 本次改造的硬约束）

### 3.5.1 双通道模型

每个功能都拆成两条并行通道，**主通道是必须存在的**：

```
                 ┌───────────────── 主通道（可见通道，必须有）
   一个功能  ────┤   按钮 / 菜单 / 图标 / 右键 / 拖拽
                 │
                 └───────────────── 加速通道（可选，好用更好）
                     Ctrl 快捷键 / 内联语法 / 命令面板 / hover 快捷位
```

**判定规则**：把加速通道**全部关掉**之后，这个功能仍然可被发现、可被完成 → 合格；否则不合格。

### 3.5.2 大众习惯对照表

| 交互习惯 | 大众熟悉度 | 典型来源 | 本案采用方式 |
|---|:--:|---|---|
| 左侧勾选框、勾选即完成 | 极高 | 所有待办 App | 保留（已有） |
| 点标题 / 双击打开详情 | 极高 | 资源管理器、手机 App | 保留（双击已有，L4353） |
| `⋯` 更多菜单 | 极高 | 微信 / Excel / 手机 App | **新增**，任务卡与项目卡统一使用 |
| 右键上下文菜单 | 高 | Windows | 保留并扩充（框架已有，L4402），**但不作唯一入口** |
| 工具栏图标**常驻**（分组 + 分隔线） | 极高 | Word / WPS | **笔记工具栏采用**，放弃 Notion 式选区浮层 |
| 回车确认 / Esc 取消 / 连续回车连加 | 极高 | 通用 | 快速添加与就地新增采用 |
| 侧栏"图标 + 文字"分组导航 | 极高 | 邮箱 / 微信 / 滴答清单 | 采用 |
| `Ctrl+` 快捷键 | 中 | Windows | **仅作加速**；且必须**同时支持 ⌘ 与 Ctrl** |
| 内联语法（`!高` `#分类` `[[链接]]`） | 低 | Todoist / Obsidian | **仅作加速**，不得替代可见入口 |
| hover 显示操作 | 低 | Linear | **仅作加速**，不得替代常驻入口 |
| 命令面板 | 低 | VSCode / Linear | **仅作加速**，不得作"新建"主入口 |

### 3.5.3 一条硬规则

> **交付前逐项核对：每一个快捷键、每一处 hover 显隐、每一处内联语法，都能在界面上找到一个"看得见、点得到"的等价入口。**
> 做不到的，要么补入口，要么删功能——不允许"藏起来的功能"存在。

---

## 4. 优化方案

### 总思路：把功能切成三档，按需露出

```
Tier 1  捕获层   →  打开就能写，零决策          （天在用）
Tier 2  组织层   →  项目 / 客户 / 标签 / 日期   （周用几次）
Tier 3  复盘层   →  日历 / 看板 / 四象限 / 报告 / 历史 / 笔记  （偶尔用）
```

现状是**三档全部铺在第一屏**，改造目标是**第一屏只有 Tier 1，Tier 2 收进侧栏分组，Tier 3 降权但仍在侧栏可见**。

---

### 改造包 A · 第一屏减负（P0，收益最大）

**A1. 快速添加条：6 个宽控件 → 1 个输入组（可见图标，不靠语法）**

- **现状**：`quickAdd()`（L2169）渲染 输入框 + 客户下拉 + 项目下拉 + 优先级下拉 + 日期 + 按钮 = 6 个宽控件
- **⚠️ 关键修正**：v1.1 曾建议"改用内联语法解析，控件降到 1"。**这是高阶用户习惯，违反新增的原则 7 / 8** —— 大众用户不会去记 `!高` / `明天`，而"藏在一个 42 字 placeholder 里的语法"更不是可发现的入口。
- **改法（可见优先）**：
  - 布局收成**一个输入组**：`[ 输入框　📅　⚑　＋ ]`
  - **日期、优先级保留为可见图标按钮**（点击弹小面板，不需要记语法），只是从"3 个占宽下拉框"变为"2 个紧凑图标"，并带 tooltip
  - **客户下拉直接删除**（由所选项目自动推导）—— 这是唯一真正被移除的控件
  - **项目选择保留在就近位置**：输入框内左侧放一个"清单选择器"（滴答清单做法），默认值 = 当前所在项目
  - 内联语法**保留但降级为加速**：placeholder 写「添加任务…」；语法提示放在**首次使用的一次性气泡**里；语法**绝不作为唯一入口**
- **预期**：宽控件 6 → 2（输入框 + ＋），**可发现性零损失**（图标可点、有提示）

**A2. 任务卡片：8 → 3 常驻 + `⋯` 菜单**

- **现状**：`taskCard`（L2477）每张卡约 8 个可点元素——勾选框、可点标题、多个可点 chip、笔记 chip、`.acts` 三个按钮（跟进/编辑/删除，L2507）
- **⚠️ 实测发现一处矛盾**：`.task .acts` 在宽屏是**常驻可见**的（L280 无 opacity 规则）；而项目卡 `.pcard .h .iact{opacity:0}`（L286）却是**悬停才显**。两者不一致，且后者属于"新用户发现不了"的隐形入口。
- **改法**：
  - 统一为**常驻「勾选框 + 标题 + `⋯`」三件套**（`⋯` 是大众最熟悉的溢出入口）
  - `⋯` 菜单内容：编辑 / 记一笔跟进 / 设日期 / 设优先级 / 标记重要 / 删除
  - **项目卡改为常驻 `⋯`**，去掉 `opacity:0` 的悬停隐藏
  - **悬停作为加速**：鼠标移入时**额外**露出"编辑 / 记跟进"两个快捷图标——有则更快，无也不影响
- **预期**：可见控件 8 → 3；同时**消除一处隐形入口**
- **注意**：这与 v1.1 的方向**相反**。v1.1 说"改成 hover 才显示"——那是把可发现的入口变成不可发现的，属于退化。

**A3. 右侧检查器：可见按钮收放 + 顺手修一个真 bug**

- **现状**：`renderInsp`（L1780）常驻；`⌘\` 切换逻辑写在 L4344
- **⚠️ 实测发现 bug**：L4343（`⌘1–8` 切视图）与 L4344（`⌘\` 开关右栏）在 `var meta=...`（L4345）**赋值之前**就使用了 `meta`。因为 var 提升所以不报错，但此处 `meta` 恒为 `undefined` → **`⌘1–8` 与 `⌘\` 实际上完全不生效**（Windows 上更明显，用户按 Ctrl 也没反应）
- **改法**：
  - 顶部加一个**可见的「详情」图标按钮**做收放（快捷键仅作加速）
  - **修正 `meta` 定义顺序**（提到所有使用点之前）—— 一行改动，零风险
  - 全部快捷键**同时绑定 `Ctrl`（Windows）与 `⌘`（macOS）**，UI 提示符号按当前平台显示

---

### 改造包 B · 导航收敛（P0）

**B1. 一级导航 10 → 5 + 次级分组（降权，但不是隐藏）**

- **现状**：今天 / 日历 / 任务 / 项目 / 看板 / 四象限 / 历史 / 总览 / 报告 / 笔记
- **⚠️ 关键修正**：v1.1 曾建议"其余收进折叠区、默认收起"。**隐藏入口同样违反原则 7** —— 大众习惯是"我要的东西都在侧栏里看得见"。
- **改法（分层 + 视觉降权，而非隐藏）**：
  - **第一组「常用」**：`今天` · `日历` · `笔记` · `任务` · `项目`（大字号、实心图标、正常色）
  - **第二组「更多视图」**：`看板` · `四象限` · `历史` · `总览` · `报告`（小字号、灰色、浅图标）
  - 两组都在侧栏内**默认可见**；「更多视图」可折叠，但**默认展开**
- **预期**：一级导航认知负荷 10 → 5 + 5 分层，**入口一个不少**

**B2. 删掉重复导航**

- **现状**：`renderTabbar()`（L1687）渲染了与左栏完全相同的 10 项，桌面端还不可见
- **改法**：直接删除；移动端如需保留，复用一个数据源
- **附带修复**：`renderMain()` 标题映射补上「项目」（L1702）

**B3. 视图与筛选物理分区**

- **现状**：视图和筛选器平铺混排（L1636–1683）
- **改法**：侧栏拆成三个带标题的分组（用大众词，不用内部术语）：
  ```
  常用      今天 · 日历 · 笔记 · 任务 · 项目
  更多视图  看板 · 四象限 · 历史 · 总览 · 报告
  整理      项目 · 客户 · 标签 · 智能列表
  ```
- **预期**：用户能一眼分辨"换页面"和"筛内容"

**B4. 统一筛选语义**

- **现状**：全局 `filtered()` 与看板 `boardTasks` 两套筛选，行为不一致
- **改法**：看板接入同一套 `filtered()`；若看板确实需要不同默认，用**显式标注的局部筛选**表达，并在 UI 上区分样式

---

### 改造包 C · 渐进披露（P1）

**C1. 修正「新建」入口：就地新增一行**

- **现状**：顶部「新建」（L1712）→ `openPalette()`（L3901），点"新建"得到搜索框
- **⚠️ 关键修正**：v1.1 建议改成"轻量捕获浮层"——仍不如**就地新增**贴合大众习惯
- **改法**：
  - 顶部「新建」→ **在当前列表顶部插入一行可编辑的输入行**（滴答清单 / 微软 To Do 的做法）
  - `回车` 保存并**保持该行** → 可连续连加（大众最常用的批量录入习惯）；`Esc` 取消
  - `⌘K` 保留为命令面板、`⌘N` 保留为全局速记 —— 但两者都降为**加速器**
- **预期**：三条路径职责清晰，主路径"看得见、点得到、回车就行"

**C2. 任务弹窗分层：12 字段 → 3 + 折叠**

- **现状**：`openTask`（L3252）一次给全 12+ 字段
- **改法**：
  - **默认显示 3 个**：标题 · 截止日期 · 项目
  - 下方放一个**看得见的**「更多选项 ▾」折叠标题（不靠快捷键展开）：优先级 / 标签 / 重要 / 状态 / 关联笔记 / 备注
  - **跟进记录与子任务不放进新建流程**，改为任务卡上的独立入口（已有 `quickLog` 内联能力）
  - 客户字段去掉，由项目自动推导
- **预期**：新建任务的心理负担从"填表格"变成"写一句话"

**C3. 项目弹窗分层：11 字段 → 3 + 折叠**

- **现状**：`openProject`（L3398）含预算/合同额/阶段/落地可能性/合同类/签署日/落地年等
- **改法**：默认显示 名称 · 颜色 · 摘要；合同/预算/阶段等一概收进**可见的**「更多选项 ▾」
- **理由**：这些字段属于"业务台账"，不是"任务管理"，不该出现在主路径

**C4. 笔记工具栏收纳：18 按钮 → 6 + "更多"（Word 式常驻）**

- **现状**：`viewNotes` 工具栏 18 个按钮平铺
- **⚠️ 关键修正**：v1.1 建议"B/I/U/S 改为选区浮层"——这是 **Notion 式新交互，大众不熟**。中国用户对"常驻工具栏图标"的熟悉度极高（Word / WPS）。
- **改法**：
  - 常驻保留 **6 个紧凑图标 + 分隔线分组**：`B` `I` `U` `S` ｜ `标题级别 ▾` ｜ `列表` `引用`
  - 其余（链接 / 代码块 / 分隔 / 预览 / 导出）收进「更多 ▾」
  - 保留 Word 式的"先选中、再点按钮"心智模型
  - 按钮加 **tooltip 显示名称与快捷键**
- **预期**：工具栏 18 → 6 + 更多，且**不引入新的交互概念**

**C5. 清理死入口与隐形入口**

| 位置 | 问题 | 处理 |
|---|---|---|
| `viewTimeline`（L2410） | 无任何入口的孤儿视图，L1701 还强制重定向 today | **按原则 7：要么给一个可见入口，要么删除**（建议删除——功能与"历史"重叠） |
| `newSmart`（L3782） | 有处理逻辑但**没有任何按钮触发** | 在「整理」分组下加一个看得见的「新建智能列表」入口，或先隐藏此分组 |
| `aiBadge`（L3040） | 前端引用了 HTML 中不存在的元素 | 删除空操作 |

---

### 改造包 D · 首次体验（P1）

**D1. 首启不灌示例数据，改用空状态引导**

- **现状**：`load()`（L1483）检测到无数据时直接写入 `demo()`（L1352）：2 客户 + 5 标签 + 3 项目 + 6 任务
- **问题**：**用户无法分辨"示例"和"我的数据"**，容易误删误改，也掩盖了产品的空状态
- **改法**：
  - 首启进入**真正的空状态**：一句话说明 + 就地输入行 + 一个「载入示例数据试用」次级按钮
  - 示例数据作为**可选动作**，且带明显标记与「清空示例」一键入口

**D2. 统一空状态**

- **现状**：空状态文案散落各视图（今天 L2247、历史 L2280、看板 L3200、笔记 L1994），样式与信息量不统一
- **改法**：抽出统一空状态组件，每处都包含 **①这里放什么 ②为什么有用 ③一个明确动作**

**D3. 一次性轻量引导（带文字，不靠悟）**

- **⚠️ 关键修正**：Linear 式"做中学、自己发现"对技术用户有效，**但对大众用户偏隐晦**
- **改法**：3 个可跳过的小步骤，**每步都带明确文字 + 指向具体的可见按钮**（例如"点这里的 📅 给任务设个日期"），完成即消失
  1. 加一条任务
  2. 给它设个日期
  3. 拖到某个项目
- 全程可跳过，不弹模态、不锁界面

---

### 改造包 E · 笔记模块升级为个人知识库（P0，用户明确的高频刚需）

> **背景**：笔记是高频使用的模块，且**后续要作为个人知识库**。这不是一个"视图"，而是一个独立的产品面。
> **重要前提**：现有笔记实现比预想的好得多——`viewNotes()`（L1949）**已经是一个三栏工作区**（分类栏 `nb-fav` | 笔记列表 `nb-list` | 编辑器 `nb-edit`），且 `noteFiltered()`（L1898）的搜索**已经能匹配正文**（`title + md`），还支持置顶、分类、导出 Markdown。**这些结构是对的，不要推翻重做。**
> **v1.2 约束**：知识库的进阶能力（双链、自动归类）**天然偏向技术型用户**。按原则 7，这些能力必须**先有可见入口、再提供语法加速**，不能反过来。

**E1. 提升为一级常驻模块（已合并进 B1）**

- 笔记与任务管理并列，位于「常用」分组第三位

**E2. 工具栏重构：18 个平铺按钮 → 6 常驻 + 分组（Word 式）**

- **现状**：`viewNotes` 工具栏（L1960–1982）平铺 18 个按钮——B / I / U / S / H1 / H2 / H3 / H4 / 列表 / 有序 / 引用 / 代码块 / 行内 / 链接 / 分隔 / 预览 / 编号 / 导出 MD
- **问题**：**H1–H4 占了 4 个按钮位，B/I/U/S 又占 4 个**，仅格式类就吃掉 8 个位置，而知识库真正高频的"引用""链接"反而被淹没
- **改法**：
  - **H1–H4 合并为一个「标题级别」下拉**（省 3 个位）
  - **B / I / U / S 保持常驻图标**（Word 习惯，**不做选区浮层**），用分隔线与后续按钮分组
  - 常驻 = `B I U S` ｜ `标题▾` ｜ `列表` `引用` = **6 个 + 1 下拉**
  - 其余（有序 / 代码块 / 行内 / 链接 / 分隔 / 预览 / 编号 / 导出 MD）收进「更多 ▾」
- **预期**：工具栏 18 → 6 + 更多，**全是用户已经会用的按钮**

**E3. 分类体系升级：单层分类 → 分类 + 多标签**

- **现状**：`note_cats(name, sort_idx)` 是**单层平铺**分类，一篇笔记的 `cat` 字段**只能属于一个分类**（schema L1442/L1447）
- **问题**：知识库的核心是"一个知识点可以被多个维度检索"。比如一篇"客户 A 的方案讨论"，它既属于「会议记录」又属于「客户 A」——现在只能二选一
- **改法**：
  - **保留现有分类**（用户已经在用，不动）
  - **新增多标签**：`notes.tags` 字段，复用任务标签的交互（**可见的 chips 多选**）
  - 分类栏在顶部加「标签」分区，与现有「全部笔记 / 未归类 / 各分类」并列
- **数据改动**：`notes` 表新增 `tags` 列。用 `main.rs` 里已有的 `ensure_col` 兼容模式追加，**不破坏老数据**

**E4. 搜索升级：命中高亮 + 片段定位**

- **现状**：`noteFiltered()`（L1898）能搜正文，但列表只显示固定 80 字的开头摘要（`noteSnip` L1911），**看不到命中位置**
- **改法**：
  - 命中词**高亮**显示
  - 摘要改为**围绕命中词截取**的片段（而不是永远取开头）
  - 显示命中数量
  - 笔记量大了以后再考虑迁到 SQLite FTS（现阶段内存过滤够用）

**E5. 双向链接与反链（知识库的分水岭）**

- **现状**：只有**单向**关联——任务可以挂一篇笔记（`task.note_id` + `openNotePicker` L3298），但**笔记之间无法互相引用**
- **问题**："笔记本 → 知识库"的关键差别就在这里。没有互相引用，笔记只能是孤立的文档堆
- **改法（可见入口优先）**：
  - 编辑器工具栏加一个**看得见的「引用笔记」按钮** → 弹出笔记搜索选择器 → 选中即插入链接（**主通道**）
  - `[[笔记标题]]` 语法保留为**加速通道**（老手可以直打）
  - 编辑器底部显示一个**常驻的「本文引用了 N 篇 / 被 N 篇引用」面板**（反链），点击可跳转
  - 未匹配的 `[[xxx]]` 显示为"待创建"，点击即新建——但不能只有语法这一条路
- **实现要点**：`md2html` 里增加 `[[...]]` 解析分支；反链可由全量扫描 `md` 实时计算（个人知识库规模下性能足够，无需额外表）

**E6. 模板与快速归类**

- **现状**：`newNote` 只创建空笔记，分类要手动在编辑器里选
- **改法（可见优先）**：
  - 新建时提供**可见的模板选择**：`空白` / `会议记录` / `读书笔记` / `周报`（点选卡片或下拉，不是让你打字）
  - 标题里 `#分类` 作为**加速语法**（回车时自动归类），与任务输入框的内联语法保持同一心智模型
  - 归类语法的提示必须出现在**可见位置**（分类选择器旁边的小字提示），不能只靠用户自己发现

**E7. 与任务的闭环**

- **现状**：任务能挂笔记，但挂完之后在任务卡上看不到笔记内容
- **改法**：任务卡上的笔记标记显示**笔记标题 + 首行摘要**，hover 可预览，点击直接跳到该笔记（`data-act="taskNotes"` 已有基础设施）
- 这样"做事"（任务）与"沉淀"（笔记）形成闭环——这正好也是当前项目 `note_id` 字段已经铺好的路

**E 包的数据改动说明（与前面几个包的区别）**

- 改造包 A / B / C / D **全部是纯前端**改动，只动 `src/index.html`
- 改造包 E 中的 **E3（多标签）** 需要给 `notes` 表加字段，属 Rust 侧改动；但因 `main.rs` 已有 `ensure_col` 兼容模式，**加列不影响老数据，也不需要迁移脚本**
- 其余 E 项（E2 工具栏、E4 搜索、E5 双链、E6 模板、E7 闭环）均为纯前端

---

## 5. 改造前后对比

| 指标 | 改造前 | 改造后 | 变化 |
|---|---:|---:|:--:|
| 一级导航项 | 10 平铺 | 5 常驻 + 5 降权 | ↓ 认知负荷 50% |
| 导航渲染次数 | 2 套 | 1 套 | ↓ 50% |
| 今天视图可点控件 | ≈30+ | ≤12 | ↓ 60% |
| 新增任务区域**宽控件** | 6 | 2 | ↓ 67% |
| 任务弹窗字段 | 12+ | 3 + 折叠 | ↓ 75% |
| 项目弹窗字段 | 11 | 3 + 折叠 | ↓ 73% |
| 笔记工具栏按钮 | 18 | 6 + 更多 | ↓ 67% |
| 添加任务步数 | 3 | 2 | ↓ 33% |
| 首启决策点 | 6（视觉上） | 1 | ↓ 83% |
| 死入口 | 3 | 0 | 清除 |
| **只能 hover 才能发现的操作** | **1 处** | **0** | 清除 |
| **失效的快捷键** | **2 组**（`⌘1–8` / `⌘\`） | **0** | 修复 |
| **每个功能都有可见鼠标入口** | 部分（依赖记忆） | **全部** | 保证 |

**笔记模块能力变化（改造包 E）**

| 能力 | 改造前 | 改造后 |
|---|---|---|
| 导航位置 | 与 9 个视图平铺 | 「常用」组第三位 |
| 工具栏 | 18 个按钮平铺 | 6 常驻 + 更多（全为既有习惯） |
| 分类维度 | 单层分类（一稿一类） | 分类 + 多标签（一稿多维度） |
| 搜索 | 能搜正文，但看不到命中位置 | 命中高亮 + 片段定位 + 计数 |
| 笔记互链 | 无（仅任务 → 笔记单向） | 可见「引用笔记」按钮 + `[[ ]]` 加速 + 反链面板 |
| 新建 | 空笔记 + 手动归类 | 模板选择 + `#分类` 加速 |
| 与任务关系 | 任务可挂笔记 | 任务卡显示笔记摘要，可跳转 |

---

## 6. 实施顺序建议

0. **第 0 步（先做，1 行改动，零风险）**：修掉 `meta` 变量提升 bug（L4343–4345），让 `Ctrl+1–8` 与 `Ctrl+\` 在 Windows 上真正生效
1. **改造包 A + B**（第一屏减负 + 导航收敛）—— 视觉改观最直接，风险最低，纯前端
2. **改造包 E 的 E2 / E4**（笔记工具栏重构 + 搜索高亮）—— 笔记是高频刚需，先把"每天都在用"的模块做顺
3. **改造包 C**（渐进披露）—— 涉及弹窗结构与入口语义调整
4. **改造包 E 的 E3 / E5 / E6 / E7**（多标签 / 双链 / 模板 / 任务闭环）—— 知识库能力的实质建设
5. **改造包 D**（首次体验）—— 放最后，避免影响前面几步的验证

**改动面说明**：除 **E3**（`notes` 表加 `tags` 列，走 `main.rs` 已有的 `ensure_col` 兼容追加）外，其余全部为纯前端 `src/index.html` 改动，随时可回滚。

---

## 7. 取舍与风险

| 风险 | 说明 | 应对 |
|---|---|---|
| 老用户不适应 | 10 个视图变成"5 常驻 + 5 降权" | **入口全部保留**，只降视觉权重；「更多视图」默认展开 |
| 高级字段被埋 | 合同/预算/阶段等移入折叠区后不好找 | 折叠区标题**常驻可见**（「更多选项 ▾」），状态按用户记忆持久化 |
| 客户自动推导出错 | 项目未归属客户时推导为空 | 保留手动覆盖入口（放在折叠区），推导仅作为默认值 |
| **保留可见控件 vs 减负目标** | 若一味靠"删控件"达标，会伤害可发现性（正是本次校正要避免的） | 用**紧凑图标 + 分组 + 视觉降权**代替删除；考核指标从"控件总数"改为"**宽控件数**"与"**视觉分组数**" |
| **老用户已习惯快捷语法** | 高手的 `!高` / `⌘K` / `[[ ]]` 被降级，可能变慢 | **一律不删**，只从"唯一入口"降为"加速通道"——双通道并行，两类用户各取所需 |
| 首启空状态可能劝退 | 首启没有内容会显得"空" | 空状态必须给明确 CTA + 一键示例数据，不能只是空白 |
| **macOS / Windows 快捷键语义不一致** | 代码按 macOS 写，但目标机可能是 Windows；`⌘` 符号会让 Windows 用户困惑 | 全部快捷键**双绑定 ⌘ 与 Ctrl**，界面提示符号按平台动态渲染 |
| **笔记模块改动波及面大** | 笔记是高频模块，改坏了直接影响日常使用；且 `[[双链]]` 会改变 `md` 正文的存储内容 | **保留现有三栏结构与分类体系不动**，只做增强；双链以纯文本 `[[]]` 存储，不写入额外结构，保证**导出 Markdown 后依然可读** |
| **模板/自动归类可能与已有笔记冲突** | 标题里带 `#` 的历史笔记会被误解析 | 归类语法只在**新建时**生效，不改动已有笔记的 `title` |

---

## 8. 验收标准

改造完成后，应同时满足：

1. 打开 App 到加入第一条任务 **≤2 步**，全程不需要看任何下拉或标签
2. **任意单个视图**同屏可点控件 **≤12 个**（宽控件更严）
3. 一个不看文档的新用户，能在 **60 秒内**独立完成"加任务 → 设今天 → 勾完成"
4. 一级导航 **≤5 项常驻**，且**笔记常驻其中**
5. 全部视图的筛选行为**语义唯一**（同一套规则）
6. 不存在**无入口的功能**和**无功能的入口**
7. **每个快捷键、每处 hover 显隐、每条内联语法，都能在界面上找到"看得见、点得到"的等价入口**（逐项核对清单，对应 §3.5.3）
8. 界面上不存在"**只有悬停才能发现**"或"**只有记语法才能触发**"的功能
9. 全部快捷键在 **Windows（Ctrl）与 macOS（⌘）** 上均可用；UI 提示符号与当前平台一致

**笔记模块（知识库）额外验收标准**

10. 笔记工具栏常驻按钮 **≤6 个**
11. 一篇笔记可以同时属于 **1 个分类 + 多个标签**
12. 笔记搜索能**高亮命中词**，并在列表中显示**围绕命中位置的片段**（而不是固定开头 80 字）
13. 正文 `[[双链]]` 可点击跳转，编辑器底部能看到**反链列表**；且**不写语法也能通过「引用笔记」按钮完成同样的操作**
14. 从任务卡可以**一跳进入关联笔记**，从笔记也能看到被哪些任务引用
