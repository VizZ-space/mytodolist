# 我的任务台 · 交互简化优化方案

> **诊断依据**：对 `my-task-desktop/src/index.html`（4400 行）与 `src-tauri/src/main.rs`（1852 行）的逐项走查，所有结论均落到具体函数与行号。
> **调研对象**：Things 3、Todoist、TickTick、Linear、Apple Reminders、Google Keep
> **日期**：2026-09-11
> **版本**：**v1.3** —— 修订：**第 0 步 + 改造包 A + 改造包 B 已落地并通过冒烟测试**，详见第 9 节《实施记录》。其中 A1 的一项设计（客户由项目推导）经代码核实**在现有数据模型下无法实施**，已调整并记录偏差。
> **历史**：v1.2 把交互范式从"高阶用户优先"校正为"大众习惯优先"；v1.1 曾把笔记误收进折叠区（已撤销）；v1.0 曾断言"纯前端改动、不碰数据库"（加入改造包 E3 后已修正）。

---

## 0. 一句话结论

这个 App 的问题**不是"流程太长"，而是"同屏选项太多、层级太多、入口语义错位"**。

实测数据显示：从打开 App 到加入第一条任务**只要 3 步**，而且**没有任何必填字段**——技术门槛其实很低。但与此同时，**今天视图同屏有 30+ 个可点控件**，一级导航 **10 项**，任务编辑弹窗 **12 个字段**，首启还直接灌入示例数据。

所以用户感觉到的"复杂"，来源不是"走不完流程"，而是**一眼看不到重点**。这不是功能问题，是**信息架构与优先级问题**。

> **v1.2 补充——第二类问题：交互范式选错了参照系。**
> 代码注释里明确写着"macOS 操作习惯"（L835、L4352），实现也大量采用 **keyboard-first / hover-hidden** 手法：导航靠 `⌘1–8`、新建靠 `⌘N`、行内操作靠悬停、格式靠语法。
> 这些是**熟练用户的加速器**，但对大部分用户来说是**隐形功能**——看不见，就不知道它存在。
> 因此简化必须遵循一个前提：**减少同屏选项，但不能把功能藏起来。** 藏起来的简化是"以牺牲可发现性换来的整洁"，对大多数用户是净损失。

---

## 1. 复杂度体检（实测数据）

| 指标 | 现状 | 健康值 | 判定 |
|---|---:|---:|:--:|
| 一级导航项 | **10** | ≤5 | 超标 2 倍 |
| 导航重复渲染 | **2 套**（左栏 + 底部 tabbar） | 1 | 冗余 |
| 今天视图可点控件 | **≈30+** | ≤12 | 严重超标 |
| 任务编辑弹窗字段 | **12+** | 3 + 折叠 | 超标 4 倍 |
| 新建项目弹窗字段 | **11** | 3 + 折叠 | 超标 |
| 笔记页工具栏按钮 | **18** | ≤6 | 超标 3 倍 |
| 报告页控件 | **25+** | ≤10 | 超标 |
| 快速添加条控件 | **6**（输入 + 3 下拉 + 日期 + 按钮） | 1 输入组 | 超标 |
| 添加任务最短步数 | 3 | ≤3 | ✅ 合格 |
| 必填字段数 | 0 | 0 | ✅ 合格 |
| 统一空状态 | 无（首启灌示例数据） | 每视图 1 个 | 缺失 |
| 首次使用引导 | 无 | 1 个轻量引导 | 缺失 |
| 死入口 / 隐形入口 | 3 处 | 0 | 缺陷 |
| **只能悬停才能发现的操作** | **1 处**（项目卡 `.pcard .h .iact{opacity:0}`，L286） | 0 | 缺陷 |
| **快捷键在 Windows 上失效** | **2 组**（`⌘1–8` 切视图、`⌘\` 开关右栏） | 0 | **bug** |

**结论：步数和必填项已经很好，真正的债在"可见控件数量"和"信息架构"。**

---

## 2. 六个根因

### R1 · 同屏控件过载（最主要）

- `viewToday` 里单个视图的可见控件 ≈30 个：快速添加条 6 + 筛选芯片 4 + 批量治理 2 + 每张任务卡 ≈8 + 完成条
- 笔记视图工具栏 **18 个按钮**平铺（`viewNotes`）
- 报告视图 25+ 控件一次性铺开（`viewReport`）
- **问题**：新用户看到的是"一整面控制面板"，而不是"一张待办清单"

### R2 · 双重导航

- 左栏 `renderSide()` 渲染 10 项视图（`tabs` 数组，L1627）
- 底部 `renderTabbar()` 又渲染**完全相同的 10 项**（L1688）
- 桌面端底栏被 `display:none` 隐藏，也就是说**这 10 项在源码里存在两遍，其中一遍永远不显示**
- 附带 bug：`renderMain()` 的标题映射表（L1702）**只有 9 项、缺「项目」**，导致进入项目视图时标题错误回退为"今天"

### R3 · 视图切换与筛选混排

- 侧栏里「今天/日历/任务/看板…」这些**视图**与「全部任务/未归类/各个项目/客户/标签/智能列表」这些**筛选器**平铺在一起，没有任何视觉分组
- 更糟的是**筛选语义不统一**：`filtered()`（L2202）是全局筛选，但**看板视图自己另走一套** `boardTasks`（L2447）
- **问题**：同一界面里"点一下换个页面"和"点一下筛掉一半内容"长得一模一样

### R4 · 入口语义错位：点「新建」打开的是命令面板

- 顶部那个显眼的「新建」按钮（L1712），点击后执行的是 `openPalette()`（L3901）
- 新用户点"新建"，**看到的是一个搜索框**
- `⌘N`（L4319）同样指向 `openPalette()`
- **问题**：命令面板是**熟练用户的加速器**，现在却承担了主入口职责

### R5 · 没有渐进披露，一次给全

- 任务弹窗 `openTask`（L3252）一次呈现 12+ 字段：标题/客户/项目/优先级/状态/日期/标签/重要/关联笔记/子任务/备注/跟进记录
- 项目弹窗 `openProject`（L3398）11 个字段：预算/合同额/阶段/落地可能性/合同类/签署日/落地年/摘要/目标……
- 弹窗里还能再开弹窗（`newClientInline` → `openClient`，L3267/L3755）
- **问题**：一个个人任务工具，却按照"合同管理系统"的字段密度设计

### R6 · 交互范式是"高阶用户优先"，不是"大众优先"（v1.2 新增）

| 证据 | 位置 | 问题 |
|---|---|---|
| 注释「阶段六：macOS 打磨（完成动效 / **克制 hover** / 对比度）」 | L835 | 整个阶段的主题就是 hover 手法 |
| `.task:hover .acts{opacity:1}` | L839 | 以悬停作为行内操作的可见条件 |
| `.pcard .h .iact{opacity:0}` / `:hover` 才 `opacity:1` | L286–287 | **项目卡操作按钮新用户根本发现不了** |
| 注释「macOS 操作习惯：双击编辑 + 右键菜单」 | L4352 | 隐藏手势 + 右键，均为"不教就不会" |
| `⌘1–8` 切视图、`⌘\` 开关右栏 | L4343–4344 | 快捷键承担导航职责 |
| `⌘K` / `⌘F` 都打开命令面板 | L4346–4347 | 主入口是面板而非可见控件 |
| `!高 !中 !低 @标签 明天` 解析能力塞在 42 字 placeholder 里 | `quickAdd` L2169 | 语法是唯一入口，且不可发现 |

**核心问题**：hover 在触屏上不存在；快捷键不可发现；语法/双链需要记忆。它们本该是**加速器**，现在却承担了**主路径**职责。

---

## 3. 市场调研：别人怎么解决同一个问题

| 产品 | 核心手法 | 可借鉴之处 |
|---|---|---|
| **Things 3** | 刻意不做日历、不做笔记、不做报告。只有 Areas → Projects → Tasks 三层；Today / Upcoming / Anytime 三个视图由日期**自动推断**，用户不需要手动分类 | **"缺少功能也是一种功能"**。视图应该由数据自动推断，而不是让用户手动切换 10 个页面 |
| **Todoist** | Inbox 兜底——**不建任何项目也能完整使用**；自然语言一次输入 `提交报告 周五 2pm @工作 #市场` 自动解析出日期+项目+标签 | **捕获必须零决策**。但注意：其语法解析是**卖点带来的学习成本**，不适合作为大众产品的主路径 |
| **Linear** | 强默认、空状态即引导、行内操作 hover 才出现、⌘K 是主要入口 | 强默认 ✅ 借鉴；**hover 与 ⌘K 属于典型的专业用户范式**，本案只可作**加速**，不可作唯一入口 |
| **Apple Reminders** | 系统级零配置，打开就能写，智能列表是可选进阶 | 个人工具的第一屏应该只有一个输入框 |
| **Google Keep** | **不要求标题、不要求分类**，点开就写，保存即完成 | 降低"启动成本"比降低"操作步骤"更重要 |
| **TickTick（滴答清单）** | 功能很全（日历/番茄/习惯/四象限），但全部靠**左侧二级入口**收纳，主区始终是任务列表；添加任务是**就在列表顶部插一行** | **最贴近中国大众用户的心智模型**，本案的导航与新增交互应以它为基准 |
| **微软 To Do** | 左侧「我的一天 / 重要 / 已计划 / 任务」+ 可自定义清单；新增任务就是**顶部一行输入框** | 导航分组与"就地新增"的直接参照 |

### 提炼出的八条设计原则

1. **捕获优先（Capture First）** —— 第一屏只回答一个问题："这条想法放哪？"其余全部后置
2. **强默认代替提问（Opinionated Defaults）** —— 能用默认值决定的事，就不要让用户选
3. **渐进披露（Progressive Disclosure）** —— 高级功能在"需要的那一刻"出现，而不是一开始摆出来
4. **空状态即引导（Empty States as Onboarding）** —— 每个空视图都要说明"这里放什么"并给一个动作
5. **导航分层** —— 一级只留天天用的 3–5 个；其余**降权但仍在**（不是消失）
6. **操作分层：常驻精简 + 溢出菜单** —— 行内只留「勾选 + 标题 + `⋯`」，其余进 `⋯` 菜单与右键菜单。**不用 hover 做唯一入口**
7. **可见通道优先（Mouse-First）** —— 每个功能都必须有"看得见、点得到"的入口；快捷键与内联语法只做加速
8. **沿用大众熟悉的既有习惯** —— 不发明新交互。左勾选框、点标题打开、`⋯` 菜单、右键菜单、工具栏图标（Word 式）、回车确认、Esc 取消、双击编辑

---

## 3.5 交互通道规范（v1.2 新增 · 本次改造的硬约束）

### 3.5.1 双通道模型

每个功能都拆成两条并行通道，**主通道是必须存在的**：

```
                 ┌───────────────── 主通道（可见通道，必须有）
   一个功能  ────┤   按钮 / 菜单 / 图标 / 右键 / 拖拽
                 │
                 └───────────────── 加速通道（可选，好用更好）
                     Ctrl 快捷键 / 内联语法 / 命令面板 / hover 快捷位
```

**判定规则**：把加速通道**全部关掉**之后，这个功能仍然可被发现、可被完成 → 合格；否则不合格。

### 3.5.2 大众习惯对照表

| 交互习惯 | 大众熟悉度 | 典型来源 | 本案采用方式 |
|---|:--:|---|---|
| 左侧勾选框、勾选即完成 | 极高 | 所有待办 App | 保留（已有） |
| 点标题 / 双击打开详情 | 极高 | 资源管理器、手机 App | 保留（双击已有，L4353） |
| `⋯` 更多菜单 | 极高 | 微信 / Excel / 手机 App | **新增**，任务卡与项目卡统一使用 |
| 右键上下文菜单 | 高 | Windows | 保留并扩充（框架已有，L4402），**但不作唯一入口** |
| 工具栏图标**常驻**（分组 + 分隔线） | 极高 | Word / WPS | **笔记工具栏采用**，放弃 Notion 式选区浮层 |
| 回车确认 / Esc 取消 / 连续回车连加 | 极高 | 通用 | 快速添加与就地新增采用 |
| 侧栏"图标 + 文字"分组导航 | 极高 | 邮箱 / 微信 / 滴答清单 | 采用 |
| `Ctrl+` 快捷键 | 中 | Windows | **仅作加速**；且必须**同时支持 ⌘ 与 Ctrl** |
| 内联语法（`!高` `#分类` `[[链接]]`） | 低 | Todoist / Obsidian | **仅作加速**，不得替代可见入口 |
| hover 显示操作 | 低 | Linear | **仅作加速**，不得替代常驻入口 |
| 命令面板 | 低 | VSCode / Linear | **仅作加速**，不得作"新建"主入口 |

### 3.5.3 一条硬规则

> **交付前逐项核对：每一个快捷键、每一处 hover 显隐、每一处内联语法，都能在界面上找到一个"看得见、点得到"的等价入口。**
> 做不到的，要么补入口，要么删功能——不允许"藏起来的功能"存在。

---

## 4. 优化方案

### 总思路：把功能切成三档，按需露出

```
Tier 1  捕获层   →  打开就能写，零决策          （天在用）
Tier 2  组织层   →  项目 / 客户 / 标签 / 日期   （周用几次）
Tier 3  复盘层   →  日历 / 看板 / 四象限 / 报告 / 历史 / 笔记  （偶尔用）
```

现状是**三档全部铺在第一屏**，改造目标是**第一屏只有 Tier 1，Tier 2 收进侧栏分组，Tier 3 降权但仍在侧栏可见**。

---

### 改造包 A · 第一屏减负（P0，收益最大）

**A1. 快速添加条：6 个宽控件 → 1 个输入组（可见图标，不靠语法）**

- **现状**：`quickAdd()`（L2169）渲染 输入框 + 客户下拉 + 项目下拉 + 优先级下拉 + 日期 + 按钮 = 6 个宽控件
- **⚠️ 关键修正**：v1.1 曾建议"改用内联语法解析，控件降到 1"。**这是高阶用户习惯，违反新增的原则 7 / 8** —— 大众用户不会去记 `!高` / `明天`，而"藏在一个 42 字 placeholder 里的语法"更不是可发现的入口。
- **改法（可见优先）**：
  - 布局收成**一个输入组**：`[ 输入框　📅　⚑　＋ ]`
  - **日期、优先级保留为可见图标按钮**（点击弹小面板，不需要记语法），只是从"3 个占宽下拉框"变为"2 个紧凑图标"，并带 tooltip
  - **客户下拉直接删除**（由所选项目自动推导）—— 这是唯一真正被移除的控件
  - **项目选择保留在就近位置**：输入框内左侧放一个"清单选择器"（滴答清单做法），默认值 = 当前所在项目
  - 内联语法**保留但降级为加速**：placeholder 写「添加任务…」；语法提示放在**首次使用的一次性气泡**里；语法**绝不作为唯一入口**
- **预期**：宽控件 6 → 2（输入框 + ＋），**可发现性零损失**（图标可点、有提示）

**A2. 任务卡片：8 → 3 常驻 + `⋯` 菜单**

- **现状**：`taskCard`（L2477）每张卡约 8 个可点元素——勾选框、可点标题、多个可点 chip、笔记 chip、`.acts` 三个按钮（跟进/编辑/删除，L2507）
- **⚠️ 实测发现一处矛盾**：`.task .acts` 在宽屏是**常驻可见**的（L280 无 opacity 规则）；而项目卡 `.pcard .h .iact{opacity:0}`（L286）却是**悬停才显**。两者不一致，且后者属于"新用户发现不了"的隐形入口。
- **改法**：
  - 统一为**常驻「勾选框 + 标题 + `⋯`」三件套**（`⋯` 是大众最熟悉的溢出入口）
  - `⋯` 菜单内容：编辑 / 记一笔跟进 / 设日期 / 设优先级 / 标记重要 / 删除
  - **项目卡改为常驻 `⋯`**，去掉 `opacity:0` 的悬停隐藏
  - **悬停作为加速**：鼠标移入时**额外**露出"编辑 / 记跟进"两个快捷图标——有则更快，无也不影响
- **预期**：可见控件 8 → 3；同时**消除一处隐形入口**
- **注意**：这与 v1.1 的方向**相反**。v1.1 说"改成 hover 才显示"——那是把可发现的入口变成不可发现的，属于退化。

**A3. 右侧检查器：可见按钮收放 + 顺手修一个真 bug**

- **现状**：`renderInsp`（L1780）常驻；`⌘\` 切换逻辑写在 L4344
- **⚠️ 实测发现 bug**：L4343（`⌘1–8` 切视图）与 L4344（`⌘\` 开关右栏）在 `var meta=...`（L4345）**赋值之前**就使用了 `meta`。因为 var 提升所以不报错，但此处 `meta` 恒为 `undefined` → **`⌘1–8` 与 `⌘\` 实际上完全不生效**（Windows 上更明显，用户按 Ctrl 也没反应）
- **改法**：
  - 顶部加一个**可见的「详情」图标按钮**做收放（快捷键仅作加速）
  - **修正 `meta` 定义顺序**（提到所有使用点之前）—— 一行改动，零风险
  - 全部快捷键**同时绑定 `Ctrl`（Windows）与 `⌘`（macOS）**，UI 提示符号按当前平台显示

---

### 改造包 B · 导航收敛（P0）

**B1. 一级导航 10 → 5 + 次级分组（降权，但不是隐藏）**

- **现状**：今天 / 日历 / 任务 / 项目 / 看板 / 四象限 / 历史 / 总览 / 报告 / 笔记
- **⚠️ 关键修正**：v1.1 曾建议"其余收进折叠区、默认收起"。**隐藏入口同样违反原则 7** —— 大众习惯是"我要的东西都在侧栏里看得见"。
- **改法（分层 + 视觉降权，而非隐藏）**：
  - **第一组「常用」**：`今天` · `日历` · `笔记` · `任务` · `项目`（大字号、实心图标、正常色）
  - **第二组「更多视图」**：`看板` · `四象限` · `历史` · `总览` · `报告`（小字号、灰色、浅图标）
  - 两组都在侧栏内**默认可见**；「更多视图」可折叠，但**默认展开**
- **预期**：一级导航认知负荷 10 → 5 + 5 分层，**入口一个不少**

**B2. 删掉重复导航**

- **现状**：`renderTabbar()`（L1687）渲染了与左栏完全相同的 10 项，桌面端还不可见
- **改法**：直接删除；移动端如需保留，复用一个数据源
- **附带修复**：`renderMain()` 标题映射补上「项目」（L1702）

**B3. 视图与筛选物理分区**

- **现状**：视图和筛选器平铺混排（L1636–1683）
- **改法**：侧栏拆成三个带标题的分组（用大众词，不用内部术语）：
  ```
  常用      今天 · 日历 · 笔记 · 任务 · 项目
  更多视图  看板 · 四象限 · 历史 · 总览 · 报告
  整理      项目 · 客户 · 标签 · 智能列表
  ```
- **预期**：用户能一眼分辨"换页面"和"筛内容"

**B4. 统一筛选语义**

- **现状**：全局 `filtered()` 与看板 `boardTasks` 两套筛选，行为不一致
- **改法**：看板接入同一套 `filtered()`；若看板确实需要不同默认，用**显式标注的局部筛选**表达，并在 UI 上区分样式

---

### 改造包 C · 渐进披露（P1）

**C1. 修正「新建」入口：就地新增一行**

- **现状**：顶部「新建」（L1712）→ `openPalette()`（L3901），点"新建"得到搜索框
- **⚠️ 关键修正**：v1.1 建议改成"轻量捕获浮层"——仍不如**就地新增**贴合大众习惯
- **改法**：
  - 顶部「新建」→ **在当前列表顶部插入一行可编辑的输入行**（滴答清单 / 微软 To Do 的做法）
  - `回车` 保存并**保持该行** → 可连续连加（大众最常用的批量录入习惯）；`Esc` 取消
  - `⌘K` 保留为命令面板、`⌘N` 保留为全局速记 —— 但两者都降为**加速器**
- **预期**：三条路径职责清晰，主路径"看得见、点得到、回车就行"

**C2. 任务弹窗分层：12 字段 → 3 + 折叠**

- **现状**：`openTask`（L3252）一次给全 12+ 字段
- **改法**：
  - **默认显示 3 个**：标题 · 截止日期 · 项目
  - 下方放一个**看得见的**「更多选项 ▾」折叠标题（不靠快捷键展开）：优先级 / 标签 / 重要 / 状态 / 关联笔记 / 备注
  - **跟进记录与子任务不放进新建流程**，改为任务卡上的独立入口（已有 `quickLog` 内联能力）
  - 客户字段去掉，由项目自动推导
- **预期**：新建任务的心理负担从"填表格"变成"写一句话"

**C3. 项目弹窗分层：11 字段 → 3 + 折叠**

- **现状**：`openProject`（L3398）含预算/合同额/阶段/落地可能性/合同类/签署日/落地年等
- **改法**：默认显示 名称 · 颜色 · 摘要；合同/预算/阶段等一概收进**可见的**「更多选项 ▾」
- **理由**：这些字段属于"业务台账"，不是"任务管理"，不该出现在主路径

**C4. 笔记工具栏收纳：18 按钮 → 6 + "更多"（Word 式常驻）**

- **现状**：`viewNotes` 工具栏 18 个按钮平铺
- **⚠️ 关键修正**：v1.1 建议"B/I/U/S 改为选区浮层"——这是 **Notion 式新交互，大众不熟**。中国用户对"常驻工具栏图标"的熟悉度极高（Word / WPS）。
- **改法**：
  - 常驻保留 **6 个紧凑图标 + 分隔线分组**：`B` `I` `U` `S` ｜ `标题级别 ▾` ｜ `列表` `引用`
  - 其余（链接 / 代码块 / 分隔 / 预览 / 导出）收进「更多 ▾」
  - 保留 Word 式的"先选中、再点按钮"心智模型
  - 按钮加 **tooltip 显示名称与快捷键**
- **预期**：工具栏 18 → 6 + 更多，且**不引入新的交互概念**

**C5. 清理死入口与隐形入口**

| 位置 | 问题 | 处理 |
|---|---|---|
| `viewTimeline`（L2410） | 无任何入口的孤儿视图，L1701 还强制重定向 today | **按原则 7：要么给一个可见入口，要么删除**（建议删除——功能与"历史"重叠） |
| `newSmart`（L3782） | 有处理逻辑但**没有任何按钮触发** | 在「整理」分组下加一个看得见的「新建智能列表」入口，或先隐藏此分组 |
| `aiBadge`（L3040） | 前端引用了 HTML 中不存在的元素 | 删除空操作 |

---

### 改造包 D · 首次体验（P1）

**D1. 首启不灌示例数据，改用空状态引导**

- **现状**：`load()`（L1483）检测到无数据时直接写入 `demo()`（L1352）：2 客户 + 5 标签 + 3 项目 + 6 任务
- **问题**：**用户无法分辨"示例"和"我的数据"**，容易误删误改，也掩盖了产品的空状态
- **改法**：
  - 首启进入**真正的空状态**：一句话说明 + 就地输入行 + 一个「载入示例数据试用」次级按钮
  - 示例数据作为**可选动作**，且带明显标记与「清空示例」一键入口

**D2. 统一空状态**

- **现状**：空状态文案散落各视图（今天 L2247、历史 L2280、看板 L3200、笔记 L1994），样式与信息量不统一
- **改法**：抽出统一空状态组件，每处都包含 **①这里放什么 ②为什么有用 ③一个明确动作**

**D3. 一次性轻量引导（带文字，不靠悟）**

- **⚠️ 关键修正**：Linear 式"做中学、自己发现"对技术用户有效，**但对大众用户偏隐晦**
- **改法**：3 个可跳过的小步骤，**每步都带明确文字 + 指向具体的可见按钮**（例如"点这里的 📅 给任务设个日期"），完成即消失
  1. 加一条任务
  2. 给它设个日期
  3. 拖到某个项目
- 全程可跳过，不弹模态、不锁界面

---

### 改造包 E · 笔记模块升级为个人知识库（P0，用户明确的高频刚需）

> **背景**：笔记是高频使用的模块，且**后续要作为个人知识库**。这不是一个"视图"，而是一个独立的产品面。
> **重要前提**：现有笔记实现比预想的好得多——`viewNotes()`（L1949）**已经是一个三栏工作区**（分类栏 `nb-fav` | 笔记列表 `nb-list` | 编辑器 `nb-edit`），且 `noteFiltered()`（L1898）的搜索**已经能匹配正文**（`title + md`），还支持置顶、分类、导出 Markdown。**这些结构是对的，不要推翻重做。**
> **v1.2 约束**：知识库的进阶能力（双链、自动归类）**天然偏向技术型用户**。按原则 7，这些能力必须**先有可见入口、再提供语法加速**，不能反过来。

**E1. 提升为一级常驻模块（已合并进 B1）**

- 笔记与任务管理并列，位于「常用」分组第三位

**E2. 工具栏重构：18 个平铺按钮 → 6 常驻 + 分组（Word 式）**

- **现状**：`viewNotes` 工具栏（L1960–1982）平铺 18 个按钮——B / I / U / S / H1 / H2 / H3 / H4 / 列表 / 有序 / 引用 / 代码块 / 行内 / 链接 / 分隔 / 预览 / 编号 / 导出 MD
- **问题**：**H1–H4 占了 4 个按钮位，B/I/U/S 又占 4 个**，仅格式类就吃掉 8 个位置，而知识库真正高频的"引用""链接"反而被淹没
- **改法**：
  - **H1–H4 合并为一个「标题级别」下拉**（省 3 个位）
  - **B / I / U / S 保持常驻图标**（Word 习惯，**不做选区浮层**），用分隔线与后续按钮分组
  - 常驻 = `B I U S` ｜ `标题▾` ｜ `列表` `引用` = **6 个 + 1 下拉**
  - 其余（有序 / 代码块 / 行内 / 链接 / 分隔 / 预览 / 编号 / 导出 MD）收进「更多 ▾」
- **预期**：工具栏 18 → 6 + 更多，**全是用户已经会用的按钮**

**E3. 分类体系升级：单层分类 → 分类 + 多标签**

- **现状**：`note_cats(name, sort_idx)` 是**单层平铺**分类，一篇笔记的 `cat` 字段**只能属于一个分类**（schema L1442/L1447）
- **问题**：知识库的核心是"一个知识点可以被多个维度检索"。比如一篇"客户 A 的方案讨论"，它既属于「会议记录」又属于「客户 A」——现在只能二选一
- **改法**：
  - **保留现有分类**（用户已经在用，不动）
  - **新增多标签**：`notes.tags` 字段，复用任务标签的交互（**可见的 chips 多选**）
  - 分类栏在顶部加「标签」分区，与现有「全部笔记 / 未归类 / 各分类」并列
- **数据改动**：`notes` 表新增 `tags` 列。用 `main.rs` 里已有的 `ensure_col` 兼容模式追加，**不破坏老数据**

**E4. 搜索升级：命中高亮 + 片段定位**

- **现状**：`noteFiltered()`（L1898）能搜正文，但列表只显示固定 80 字的开头摘要（`noteSnip` L1911），**看不到命中位置**
- **改法**：
  - 命中词**高亮**显示
  - 摘要改为**围绕命中词截取**的片段（而不是永远取开头）
  - 显示命中数量
  - 笔记量大了以后再考虑迁到 SQLite FTS（现阶段内存过滤够用）

**E5. 双向链接与反链（知识库的分水岭）**

- **现状**：只有**单向**关联——任务可以挂一篇笔记（`task.note_id` + `openNotePicker` L3298），但**笔记之间无法互相引用**
- **问题**："笔记本 → 知识库"的关键差别就在这里。没有互相引用，笔记只能是孤立的文档堆
- **改法（可见入口优先）**：
  - 编辑器工具栏加一个**看得见的「引用笔记」按钮** → 弹出笔记搜索选择器 → 选中即插入链接（**主通道**）
  - `[[笔记标题]]` 语法保留为**加速通道**（老手可以直打）
  - 编辑器底部显示一个**常驻的「本文引用了 N 篇 / 被 N 篇引用」面板**（反链），点击可跳转
  - 未匹配的 `[[xxx]]` 显示为"待创建"，点击即新建——但不能只有语法这一条路
- **实现要点**：`md2html` 里增加 `[[...]]` 解析分支；反链可由全量扫描 `md` 实时计算（个人知识库规模下性能足够，无需额外表）

**E6. 模板与快速归类**

- **现状**：`newNote` 只创建空笔记，分类要手动在编辑器里选
- **改法（可见优先）**：
  - 新建时提供**可见的模板选择**：`空白` / `会议记录` / `读书笔记` / `周报`（点选卡片或下拉，不是让你打字）
  - 标题里 `#分类` 作为**加速语法**（回车时自动归类），与任务输入框的内联语法保持同一心智模型
  - 归类语法的提示必须出现在**可见位置**（分类选择器旁边的小字提示），不能只靠用户自己发现

**E7. 与任务的闭环**

- **现状**：任务能挂笔记，但挂完之后在任务卡上看不到笔记内容
- **改法**：任务卡上的笔记标记显示**笔记标题 + 首行摘要**，hover 可预览，点击直接跳到该笔记（`data-act="taskNotes"` 已有基础设施）
- 这样"做事"（任务）与"沉淀"（笔记）形成闭环——这正好也是当前项目 `note_id` 字段已经铺好的路

**E 包的数据改动说明（与前面几个包的区别）**

- 改造包 A / B / C / D **全部是纯前端**改动，只动 `src/index.html`
- 改造包 E 中的 **E3（多标签）** 需要给 `notes` 表加字段，属 Rust 侧改动；但因 `main.rs` 已有 `ensure_col` 兼容模式，**加列不影响老数据，也不需要迁移脚本**
- 其余 E 项（E2 工具栏、E4 搜索、E5 双链、E6 模板、E7 闭环）均为纯前端

---

## 5. 改造前后对比

| 指标 | 改造前 | 改造后 | 变化 |
|---|---:|---:|:--:|
| 一级导航项 | 10 平铺 | 5 常驻 + 5 降权 | ↓ 认知负荷 50% |
| 导航渲染次数 | 2 套 | 1 套 | ↓ 50% |
| 今天视图可点控件 | ≈30+ | ≤12 | ↓ 60% |
| 新增任务区域控件 | 6（且换行成"表单"观感） | 5（**单行**、更紧凑） | 观感为主 |
| 任务弹窗字段 | 12+ | 3 + 折叠 | ↓ 75% |
| 项目弹窗字段 | 11 | 3 + 折叠 | ↓ 73% |
| 笔记工具栏按钮 | 18 | 6 + 更多 | ↓ 67% |
| 添加任务步数 | 3 | 2 | ↓ 33% |
| 首启决策点 | 6（视觉上） | 1 | ↓ 83% |
| 死入口 | 3 | 0 | 清除 |
| **只能 hover 才能发现的操作** | **1 处** | **0** | 清除 |
| **失效的快捷键** | **2 组**（`⌘1–8` / `⌘\`） | **0** | 修复 |
| **每个功能都有可见鼠标入口** | 部分（依赖记忆） | **全部** | 保证 |

**笔记模块能力变化（改造包 E）**

| 能力 | 改造前 | 改造后 |
|---|---|---|
| 导航位置 | 与 9 个视图平铺 | 「常用」组第三位 |
| 工具栏 | 18 个按钮平铺 | 6 常驻 + 更多（全为既有习惯） |
| 分类维度 | 单层分类（一稿一类） | 分类 + 多标签（一稿多维度） |
| 搜索 | 能搜正文，但看不到命中位置 | 命中高亮 + 片段定位 + 计数 |
| 笔记互链 | 无（仅任务 → 笔记单向） | 可见「引用笔记」按钮 + `[[ ]]` 加速 + 反链面板 |
| 新建 | 空笔记 + 手动归类 | 模板选择 + `#分类` 加速 |
| 与任务关系 | 任务可挂笔记 | 任务卡显示笔记摘要，可跳转 |

---

## 6. 实施顺序建议

0. **第 0 步（先做，1 行改动，零风险）**：修掉 `meta` 变量提升 bug（L4343–4345），让 `Ctrl+1–8` 与 `Ctrl+\` 在 Windows 上真正生效
1. **改造包 A + B**（第一屏减负 + 导航收敛）—— 视觉改观最直接，风险最低，纯前端
2. **改造包 E 的 E2 / E4**（笔记工具栏重构 + 搜索高亮）—— 笔记是高频刚需，先把"每天都在用"的模块做顺
3. **改造包 C**（渐进披露）—— 涉及弹窗结构与入口语义调整
4. **改造包 E 的 E3 / E5 / E6 / E7**（多标签 / 双链 / 模板 / 任务闭环）—— 知识库能力的实质建设
5. **改造包 D**（首次体验）—— 放最后，避免影响前面几步的验证

**改动面说明**：除 **E3**（`notes` 表加 `tags` 列，走 `main.rs` 已有的 `ensure_col` 兼容追加）外，其余全部为纯前端 `src/index.html` 改动，随时可回滚。

---

## 7. 取舍与风险

| 风险 | 说明 | 应对 |
|---|---|---|
| 老用户不适应 | 10 个视图变成"5 常驻 + 5 降权" | **入口全部保留**，只降视觉权重；「更多视图」默认展开 |
| 高级字段被埋 | 合同/预算/阶段等移入折叠区后不好找 | 折叠区标题**常驻可见**（「更多选项 ▾」），状态按用户记忆持久化 |
| 客户自动推导出错 | 项目未归属客户时推导为空 | 保留手动覆盖入口（放在折叠区），推导仅作为默认值 |
| **保留可见控件 vs 减负目标** | 若一味靠"删控件"达标，会伤害可发现性（正是本次校正要避免的） | 用**紧凑图标 + 分组 + 视觉降权**代替删除；考核指标从"控件总数"改为"**宽控件数**"与"**视觉分组数**" |
| **老用户已习惯快捷语法** | 高手的 `!高` / `⌘K` / `[[ ]]` 被降级，可能变慢 | **一律不删**，只从"唯一入口"降为"加速通道"——双通道并行，两类用户各取所需 |
| 首启空状态可能劝退 | 首启没有内容会显得"空" | 空状态必须给明确 CTA + 一键示例数据，不能只是空白 |
| **macOS / Windows 快捷键语义不一致** | 代码按 macOS 写，但目标机可能是 Windows；`⌘` 符号会让 Windows 用户困惑 | 全部快捷键**双绑定 ⌘ 与 Ctrl**，界面提示符号按平台动态渲染 |
| **笔记模块改动波及面大** | 笔记是高频模块，改坏了直接影响日常使用；且 `[[双链]]` 会改变 `md` 正文的存储内容 | **保留现有三栏结构与分类体系不动**，只做增强；双链以纯文本 `[[]]` 存储，不写入额外结构，保证**导出 Markdown 后依然可读** |
| **模板/自动归类可能与已有笔记冲突** | 标题里带 `#` 的历史笔记会被误解析 | 归类语法只在**新建时**生效，不改动已有笔记的 `title` |

---

## 8. 验收标准

改造完成后，应同时满足：

1. 打开 App 到加入第一条任务 **≤2 步**，全程不需要看任何下拉或标签
2. **任意单个视图**同屏可点控件 **≤12 个**（宽控件更严）
3. 一个不看文档的新用户，能在 **60 秒内**独立完成"加任务 → 设今天 → 勾完成"
4. 一级导航 **≤5 项常驻**，且**笔记常驻其中**
5. 全部视图的筛选行为**语义唯一**（同一套规则）
6. 不存在**无入口的功能**和**无功能的入口**
7. **每个快捷键、每处 hover 显隐、每条内联语法，都能在界面上找到"看得见、点得到"的等价入口**（逐项核对清单，对应 §3.5.3）
8. 界面上不存在"**只有悬停才能发现**"或"**只有记语法才能触发**"的功能
9. 全部快捷键在 **Windows（Ctrl）与 macOS（⌘）** 上均可用；UI 提示符号与当前平台一致

**笔记模块（知识库）额外验收标准**

10. 笔记工具栏常驻按钮 **≤6 个**
11. 一篇笔记可以同时属于 **1 个分类 + 多个标签**
12. 笔记搜索能**高亮命中词**，并在列表中显示**围绕命中位置的片段**（而不是固定开头 80 字）
13. 正文 `[[双链]]` 可点击跳转，编辑器底部能看到**反链列表**；且**不写语法也能通过「引用笔记」按钮完成同样的操作**
14. 从任务卡可以**一跳进入关联笔记**，从笔记也能看到被哪些任务引用

---

## 9. 实施记录

### 2026-09-11 · 第 0 步 + 改造包 A + 改造包 B（已落地）

改动全部在 `my-task-desktop/src/index.html` 单文件内，**+101 / −38 行**。已通过 `vite build` 与**无头 Chrome 冒烟测试**（零 JS 报错）。

| 项 | 实际做了什么 | 冒烟测试证据 |
|---|---|---|
| **第 0 步** | 把 `var meta=e.metaKey\|\|e.ctrlKey` 提到使用点之前（原先被 var 提升坑掉） | 页面内派发 `Ctrl+3` → 标题变「笔记」、`Ctrl+9` → 「总览」、`Ctrl+\` → 右栏收缩。**改前这三组完全不响应** |
| **A1** | placeholder 42 字 → 6 字；内联语法降级为 hover 提示；控件压成单行；优先级标签由「低 普通/中 重要/高 最高」缩短为「普通/重要/最高」 | `placeholder` = "添加任务，回车即可"；`title` 含完整语法说明；3 个下拉 + 日期 + 按钮齐全 |
| **A2** | 任务卡与项目卡：3 个常驻图标 → **1 个常驻 `⋯`**；移除项目卡 `opacity:0` 的悬停隐藏 | 每张卡 `.acts .iact` 由 3 → 1；项目卡隐藏图标数 = 0 |
| **A2 配套** | 新增 `runSyntheticAct(act,id,root)`：菜单改为按「动作 + id」直接走主派发器，**不再依赖卡内是否存在同名按钮**（原实现一变卡片结构菜单就变哑） | 「标记完成」完成数 2→3；「记一笔跟进」弹出输入框；「打开完整编辑」弹出表单；「删除任务」弹出确认框 |
| **A3** | 快捷键提示符号按平台渲染：Windows 显示 `Ctrl+`，macOS 显示 `⌘` | 搜索框实测 `搜索任务  Ctrl+F` |
| **B1** | 导航分「常用」5 项 +「更多视图」5 项，后者小字降权，但**全部默认可见**（不是折叠隐藏） | 侧栏标题实测：`常用` / `更多视图`；`[data-act=view]` 共 10，其中 `.minor` = 5 |
| **B2** | 删除重复的 `tabs` 数组，侧栏与移动端底栏共用 `NAV_ALL` 一份数据源 | 全文件已无 `var tabs=[[...` 与 `tabs.map`；`#tabbar` 仍有 10 项（移动端未受影响） |
| **B2 附带修复** | `renderMain` 标题映射表改用 `NAV_ALL`，顺带修掉「项目」视图标题丢失的 bug | 切到项目视图后标题实测为「项目」 |
| **B4** | `boardTasks()` 改为以 `filtered()` 为基础，看板不再自走一套筛选语义 | 看板渲染 9 张卡、无报错；筛选条加 `title` 标注「本页筛选」 |

### 与初版方案的偏差（v1.3 更新）

1. **A1「客户由项目自动推导」—— 已解除阻塞并落地（v1.3）。**
   初版核实发现 `projects` 表没有 `client_id` 列，因此第一轮把客户下拉保留为可见入口。
   本轮按当时给出的可选方案落地：走 `main.rs` 既有的 `ensure_col` 兼容追加，给 `projects` 加 `client_id TEXT`
   （老库无损、老数据该列为 NULL，前端按空串处理），并在 `proj_sig` / `read_full` / 项目 upsert 三处同步扩列。
   于是推导成立：快速添加行**删掉客户下拉**，改为**看得见的推导回显**（`.qcli` + 隐藏输入 `#qc`）；
   任务详情里保留「手动指定客户」按钮用于覆盖推导结果。
   **这是本轮唯一的数据库结构变更，且是向后兼容的追加列，不需要迁移脚本。**

2. **A3 的可见入口本来就存在。**
   顶部早已有「收起/展开面板」按钮（`renderMain` 的 `toggleInsp`），所以 A3 实际只需修 bug + 平台符号，不需要新增按钮。

3. **A2 未削减可点 chip。**
   任务卡上的「★ 重要」「N 篇笔记」等 chip 保持可点（单击即达，是有效的快捷入口，且符合"可见入口优先"）。本轮只收敛了右下角操作区（3 → 1）。

---

### 2026-09-11 · 改造包 C + 改造包 D + 改造包 E（已落地）

改动集中在 `my-task-desktop/src/index.html`，另有 `src-tauri/src/main.rs`（`projects.client_id`、`notes.tags` 两个兼容追加列）。
已通过 `vite build` 与**无头 Chrome 冒烟测试：26 项断言全绿、0 条 JS 运行时错误**。

**测试脚本**：`my-task-desktop/.smoke/smoke.mjs`（本地脚本，依赖本机 Chrome，已在 `.gitignore` 排除，不入库）。
它全部走**用户看得见的入口**（点按钮 / 敲回车）来断言，不读模块内部变量——因为源码是 `type="module"`，从外部拿不到 `db` / `ui`，这也顺带保证了"入口真的可用"。

#### 改造包 C · 渐进披露（P1）

| 项 | 实际做了什么 | 冒烟测试证据 |
|---|---|---|
| **C1** | 「新建」不再打开命令面板，改为**就地聚焦列表顶部的输入行**（`inlineCapture()`）；`⌘K` 仍保留面板作为加速通道；Esc 收起该行 | 点「新建」后 `document.activeElement.id==='qt'`，且 `#palette` 未打开 |
| **C2** | 任务详情由 12+ 字段压到**默认 4 项**（标题 / 截止日期 / 项目 / 客户推导行），其余（优先级 / 状态 / 标签 / 重要 / 关联笔记 / 子任务 / 备注 / 跟进）收进**常驻可见**的 `details#taskMore「更多选项 ▾」` | 默认可见字段 = 4；`#taskMore` 是 `<details>` 且默认 `open=false`；展开后含 优先级 / 状态 / 关联笔记 |
| **C2 客户** | 快速添加行删除客户下拉，改为 `.qcli` 推导回显 + 隐藏 `#qc`；`qproj` / `tproj` / `pclient` 变更时重新推导 | `.qcli` 存在、`#qc` 存在、客户下拉数量 = 0 |
| **C3** | 项目弹窗同样分层：默认 4 项（名称 / 客户 / 颜色 / 摘要）+ `details#projMore「更多选项 ▾」`（阶段 / 预算 / 合同 / 落地 / 核心目标）；保存时把客户同步给该项目下的任务 | 默认可见字段 = 4；`#projMore` 折叠；默认层含「客户」 |
| **C5** | 删除孤儿视图 `viewTimeline`（无任何入口）与引用了不存在元素的 `updateAiBadge`；补上「智能列表 · 新建」入口与编辑铅笔（原 `openSmart` 是死代码） | 侧栏已无 `[data-id="timeline"]`；`[data-act="newSmart"]` 存在且文本含「智能列表」 |

#### 改造包 D · 首次体验（P1）

| 项 | 实际做了什么 | 冒烟测试证据 |
|---|---|---|
| **D1** | 全新工作台**不再自动塞 6 条假数据**（`load()` 改为 `emptyData()`），而是诚实地空着；空态给「载入示例数据」按钮，载入后顶部常驻「当前是示例数据」横幅 + 一键清空 | 首启 `.demo-hint` 有入口且任务数 = 0；载入后出现 `.demo-banner` 且任务数 > 0；清空后回到首启态 |
| **D2** | 统一空状态组件 `emptyState()`：每个空状态都回答 ①这里放什么 ②为什么有用 ③现在能做什么；替换了「今天 / 历史 / 日历 / 全部 / 项目」里散落的裸空 `div` | `.estate` 同时含标题、说明、主行动按钮 |
| **D3** | 3 步上手引导条（加一条任务 → 设日期 → 归项目），文字明确指向具体可见按钮；`localStorage.onboardDone` 记录跳过 | 加一条任务后出现 `.ob-strip`（3 步）；点「不用了」后消失且标记写入 |

#### 改造包 E · 笔记升级为个人知识库（P0）

| 项 | 实际做了什么 | 冒烟测试证据 |
|---|---|---|
| **E1** | 笔记与任务管理并列，位于「常用」分组第三位（已在 B1 一并完成） | 侧栏 `[data-act="view"][data-id="notes"]` 存在 |
| **E2** | 笔记工具栏 18 个平铺按钮 → **8 个常驻**（B I U S · 标题级别下拉 · 列表 · 引用段落 · 引用笔记）+ 分隔线 + `details#ntMore「更多 ▾」`（有序 / 代码块 / 行内代码 / 插入外部链接 / 分隔线 / 预览 / 自动章节编号 / 导出） | 常驻 = 8；`#noteHSel` 存在；`#ntMore` 默认折叠且内含 8 个按钮；「引用笔记」可见 |
| **E3** | 笔记支持**多标签**（复用任务标签体系）：`noteTagIds` / `noteTagChipsHtml`，chip 点选即挂上/摘掉，「＋ 新建标签」内联创建；左侧分类栏出现「标签」分区可筛选 | 新建标签后 chip 变 `.on`；左侧出现 `[data-act="noteTag"]` 项 |
| **E4** | 笔记搜索命中**高亮**（`hlText` → `<mark class="nhit">`）+ 显示「N 处匹配」；`noteSnippet` 截取命中上下文而非固定开头 | 搜索「会议」→ `.nb-list mark.nhit` 存在且 `.ni-hits` 含「处匹配」 |
| **E5** | 双链：`[[笔记标题]]` 解析为可点 `a.wikilink`（未创建显示「待创建」可一键新建）；解析出链 + 反链，面板常驻编辑器下方；可见入口「引用笔记」 | 点「引用笔记」选一篇 → 面板「本文引用了 1 篇」+ 编辑器出现 `a.wikilink`；切到被引用那篇 → 「被 N 篇引用」 |
| **E6** | 4 个可见模板（空白 / 会议记录 / 读书笔记 / 周报，`{{date}}` 自动替换）；标题结尾写 `#分类` 回车自动归类，提示就放在分类选择器旁 | 「模板 ▾」→「会议记录」→ 正文骨架含「会议记录」 |
| **E7** | 任务 ↔ 笔记闭环：任务卡笔记徽标显示**笔记标题**（单篇点击直达、多篇弹出清单），而不是"N 篇笔记" | 随 C2 / D2 一起验证渲染无报错 |

#### 本轮顺带修掉的三个真 bug

1. **`insertWikiLink` 在无选区时会丢链接（「引用笔记」点了没反应）。**
   原实现在"光标不在编辑器内"时先写 `ed.innerHTML`，紧接着又被 `ed.innerHTML=md2html(nn.md)`（旧 md）覆盖，插入的链接直接消失。
   现改为：光标在编辑器内就**就地插入**；否则**安全追加到末尾**；且**绝不用 `deleteContents()` 去动编辑器之外的选区、绝不整段重写已有内容**。
   光标最终落在链接**之后**（而不是链接内部），方便继续输入。

2. **`notes.tags`（E3 多标签）在 Rust 侧根本没落库 —— 真实的数据丢失。**
   `notes` 表的读 / 写 SQL 都不含 `tags`，表定义里也没有该列，前端辛苦挂上的标签一存一读就没了。
   现已补上：`CREATE TABLE` 加列 + `ensure_col(conn,"notes","tags","TEXT")` 兼容追加 + 两处读、一处写、`note_sig` 全部带上 `tags`（与 `tasks.tags` 一样存 JSON 数组字符串）。

3. **E2 工具栏注释与实际不符**（原文写「6 个常驻」，实际 8 个），已更正，避免后续维护被误导。

#### 验收对照（方案 §8）

| 验收项 | 结果 |
|---|---|
| 首屏常驻控件数量显著下降 | A2：卡片操作区 3 → 1；C2/C3：弹窗默认字段 12+ → 4 |
| 每个功能都有可见、可点的入口 | C1 就地新增、C2/C3「更多选项 ▾」常驻、E5「引用笔记」按钮、E6「模板 ▾」下拉、D1「载入示例数据」按钮 —— 冒烟测试逐项点过 |
| 键盘 / 语法只作加速通道 | `⌘K` 面板、`[[ ]]`、`#分类`、内联 `!高 / @标签 / 明天` 均保留，但都不是唯一路径 |
| 首次启动不自欺 | D1：空的就是空的，示例数据必须用户主动载入，且全程有「示例数据」横幅 |
| 零 JS 运行时错误 | 冒烟测试全程 0 条（`Runtime.exceptionThrown` + `Log.entryAdded` 双通道采集） |

#### 剩余待做（未实现，供你决定优先级）

- **笔记分级目录树**：当前是「分类 + 标签」二维，尚未做无限层级目录。
- **笔记全文检索下沉到 SQLite FTS**：当前是内存过滤，笔记量大了再说（方案 E4 已注明"现阶段内存过滤够用"）。
- **任务卡 chip 数量**：A2 只收敛了右下角操作区；chip 仍较多，但按"可点即入口"原则刻意保留。
- **改造包 C4 / D4 / E8 等方案中未编号细项**：如需继续，请直接点名。

---

### 2026-09-11 · 第二轮：一致性验证 + 三阶段代码审查 + 死代码清理 + import 规范化

#### 1. 业务逻辑一致性验证 —— 结论：与原始设计思路一致

对照 `overview.md`（原始 4 大模块）、`todo-app-design-research.md`（P0/P1 优先级与"客户=Area / 项目=Project"的归类模型）、本方案 §3 八条原则 / §3.5 双通道硬约束 / §8 验收标准，逐项核对当前实现：

| 原始设计意图 | 当前实现 | 判定 |
|---|---|---|
| 任务可拆子任务、子任务回算进度 | 保留（`subStatusHtml` / `subStatusOf`） | ✅ |
| 任务可关联项目；客户为项目上层 | `projects.clientId` 已加；快速添加行客户下拉→推导回显 | ✅ |
| 标签跨项目横向分类（调研 P1） | 任务标签保留，并复用为笔记多标签（E3） | ✅ 超额 |
| 日历视图（调研 P1） | `viewCalendar` + `calMonth`/`calWeek` | ✅ |
| 收藏箱式捕获优先 | C1 就地新增；`⌘K` 降为加速通道 | ✅ |
| 强默认代替提问 | 任务/项目弹窗默认 3–4 项，其余进可见折叠 | ✅ |
| 原则 7：每个功能都有可见入口 | 冒烟逐项点过；新增 `data-act` 审计 = **0 死入口** | ✅ |
| 原则 5：导航 ≤5 常驻且笔记在列 | `NAV_COMMON` 5 项（含笔记）+ `NAV_MORE` 5 项降权可见 | ✅ |
| D1 首启不自欺 | `load()` → `emptyData()`，示例需主动载入 | ✅ |

**发现并记录的行为级偏差（非缺陷，均为本轮之前的设计取舍）：**
1. C2 方案原文要求"跟进记录与子任务不放进新建流程"，实现是**收进 `details#taskMore`** 而非移出弹窗。因折叠区常驻可见，"默认 4 项"的验收指标仍然满足，故按 §7 风险表的"折叠区标题常驻可见"应对之策保留。
2. C2 客户字段未直接删除，而是"推导回显 + 手动指定覆盖"（已在 §9 第一轮记录，原因是项目未指定客户时会出现无归属）。

#### 2. 三阶段代码审查（按用户指定技能执行）

审查范围：`92a5dd3..HEAD` 的 `src/index.html` 与 `src-tauri/src/main.rs`。

**阶段一 · code-simplifier** —— 产出死代码清单 + 简化建议，并**明确指出哪些不该改**（`openProject` 的草稿映射、非显而易见的 bug 修复注释、"可点即入口"的 chip 均判定为"不简化"）。

**阶段二 · requesting-code-review** —— 按 code-reviewer 模板派发独立审查，得到 1 Critical / 2 Important / 3 Minor。

**阶段三 · receiving-code-review** —— 逐条**先验证再实施**，未采纳任何未经验证的建议：

| 审查意见 | 验证过程 | 裁定 |
|---|---|---|
| **Critical：`inlineMd` 双链对笔记标题未转义 → 存储型 XSS** | 读码发现 `inlineMd` 开头即 `s=esc(s)`（转义**早于** `[[ ]]` 提取）；并写 `.smoke/xss-probe.mjs` 用真实浏览器验证：注入标题 `a" onmouseover="…` 后 `getAttribute('onmouseover')===null`、`window.__pwn===0`，实体编码的引号无法终止属性值 | **驳回**（误报） |
| Important：`projects.client_id` 缺回归测试 | 核实 8 个测试中确实只覆盖 `notes.tags` | **采纳** → 新增 2 个测试 |
| Important：删除标签后笔记的 `tags` 仍留孤儿 id | 核实 `delTag` 只清 `db.tasks`、漏了 `db.notes` | **采纳** → 修 `delTag` + `normalizeAll` 兜底 |
| Minor：`emptyState` 的 `why` 未转义 | 核实所有调用方传静态串（且含刻意 `<br>`），`esc()` 反而会破坏渲染 | **不采纳**，改为在函数处注明"只接受可信静态串"的契约 |
| Minor：颜色值直接拼进 `style` 属性 | 核实 `pickColor` 只出固定调色板，但 `normalizeProject` 接受任意字符串 → 经"导入备份"可达 | **采纳** → 新增 `safeColor()` 在数据入口收敛为 `#hex` |
| Minor：`insertWikiLink` 就地插入的是裸文本，需等下次重渲染才变成链接 | 读码确认；且此分支此前**无任何用例覆盖** | **采纳** → 改为直接插入 `<a class="wikilink">`，并补冒烟用例 |

#### 3. 死代码清理（已删除 120 行）

| 类别 | 内容 |
|---|---|
| JS 零引用函数（4） | `reorderTask`（拖拽重排从未接线）、`relLabel`、`miniCal`、`weekRange` |
| JS 未用变量（1） | `SUBST`（同行的 `subStatusOf` 才是活的） |
| 图标（1） | `IC.tl`（时间线图标） |
| CSS 规则（约 40 条） | 已删视图的遗留：`.tl*`（时间线，C5 删了 `viewTimeline`）、`.forecast*`（从未实现的预测视图）、`.tabs-row/.th-tabs*/.th-tools*/.tabs-tools*/.top-tools`（B2 删掉的重复导航，含 @media 覆盖）、`.minical/.mc-*`（`miniCal` 的样式）、`.ntb-export`（E2 换成 `.ntb-tx`）、`.spill*`、`.chip.wait`、`.chip.rep`、`.sec-box.wait`、`.frow`、`.dpies/.dpie/.dpl`、`.rsel`、`.btoggle`、`.cdots`、`.note-empty`、`.cat-def`、`.sel-pin`、`.subdue-input`、`.subpri-input`、`.tabs-row` |
| Rust 零引用函数（1） | `autostart_reg_key`（Windows 注册表路径拼装，已被 `set_autostart_windows` 内联字面量取代） |

全部经"精确整块文本 + 唯一命中"校验后删除（`.smoke/prune.mjs`），删除后静态审计：**零引用函数 0、零引用变量 0、未用图标 0、未用 CSS 类 0、死入口 0**。

> 注：未被采纳的"未使用"告警中，`Menu` / `MenuItem` / `PredefinedMenuItem` / `TrayIconBuilder` / `TrayIconId` / `escape_osascript` 经核实**只在 `#[cfg(target_os = "macos")]` 分支使用**，属跨平台误报。处理方式是**按平台门控导入/定义**（而不是删除），确保 macOS 构建不被破坏。

#### 4. R7 代码规范：Rust 全路径引用改为 `use` 导入

把内联全路径改为 `use` 导入，共 **181 处**：
`serde_json::Value/json`（105）、`rusqlite::params`（34）、`std::fs::`（17）、`tauri::AppHandle`（11）、`std::process::Command`（8）、`std::env::`（5）、`std::sync::mpsc::channel`（4）、`std::path::Path::new`（3）、`std::time::Duration`（1）；并合并 `use` 块、删除 2 处函数内重复的局部 `use`。
保留 `serde_json::from_str`、`rusqlite::Result`、`#[tauri::command]`、`tauri::Wry` 等单次使用且属惯例的限定路径。

#### 5. 本轮验证结果

| 验证项 | 结果 |
|---|---|
| `vite build` | ✅ 通过 |
| 无头 Chrome 冒烟（`.smoke/smoke.mjs`） | ✅ **27/27**（新增 1 条"光标在正文内插入双链"用例），**0 条 JS 运行时错误** |
| `cargo test --no-default-features` | ✅ **10/10**（新增 `project_client_id_roundtrip`、`old_projects_table_upgrade_adds_client_id`），**0 告警** |
| 静态审计（`.smoke/audit.mjs`） | ✅ 死入口 0 / 零引用函数 0 / 零引用变量 0 / 未用图标 0 / 未用 CSS 0 |

**规模变化**：`index.html` 5159 → 5090 行（−69）；`main.rs` 2039 → 2094 行（净增，含新增 2 个测试，同时因 import 收紧而略降体积）。

---

### 2026-09-15 · 信息架构重构：今天 / 任务 / 看板 三入口去重

**触发**：用户反馈「今天和任务重复了，这两个都是待办事项的入口」；并希望把看板提到最上方。

**诊断**：改造包 B 虽然把导航分成「常用 / 更多视图」两档，但**没有消除同源入口**——「今天」「任务」「看板」三者渲染的是同一份任务库：

- 「今天」= 快速添加 + 优先级筛选条 + 今天要处理 + 久未跟进 + **「进行中的任务」全量列表**
- 「任务」= 快速添加 + 优先级筛选条 + 全量列表（进行中 / 已完成）
- 「看板」= 快速添加 + 项目筛选条 + 同一份任务按状态分三列

其中「今天」底部的全量列表与「任务」页完全重复；「看板」只是同一份数据的另一种呈现方式，却占了一个与「任务」平级的导航位。

**原则**：一个任务库、多个切面。**呈现方式是页面内的切换器，不是导航项；导航项只应区分"数据范围/心智"。**

**落地**：

| 改动 | 说明 |
|---|---|
| 新增 `ui.taskMode`（`list`/`board`） | 「任务」页的呈现方式，默认 `list` |
| 新增 `taskModeSwitch()` | 渲染在**任务页最上方**（在快速添加行与筛选条之前）的 `列表 / 看板` 分段切换器，是进入看板的唯一入口 —— 即用户所说的"把看板放到最上方" |
| 导航移除「看板」 | 「更多视图」5 项 → 4 项（四象限 / 历史 / 总览 / 报告），`NAV_ALL` 变 9 项，快捷键 1–9 顺延；移动端底部导航同步 |
| 删除 `viewBoard()` | 能力并入 `viewAll()`：切换器 → 快速添加 → 筛选条（看板模式用 `boardFilterBar`）→ `kanban()` / 列表 |
| 「今天」瘦身 | 去掉优先级筛选条与「进行中的任务」全量列表，改为一行引导 `.today-more`：「去『任务』看全部 N 项进行中」。今天只保留时间焦点：今天要处理 / 逾期批量治理 / 近 3 天 / 久未跟进 / 已完成入口 |
| 命令面板 | ⌘K 的「看板」改为 `视图=任务 + 呈现=看板`；「任务」强制回列表形态 |
| `kanban()` 去掉重复标题 | 「任务看板」标题与拖拽说明已由顶部切换器承担 |
| `priChips()` 抽取 | 列表与看板两条筛选条共用同一份优先级芯片；顺带修复看板模式下芯片**没有颜色**（与列表不一致）的问题 —— 与用户上一次提的"优先级芯片需要颜色"同源 |
| CSS 清理 | 删除 `.today-rest` 三条孤儿规则；新增 `.today-more`、`.tmode-head`、`.tmode-seg` |

**取舍说明**：

- 「今天」不再显示无截止日期的任务。这是**有意为之**——那些任务属于"全部任务"，在「任务」页看。留一行入口保证一步可达（并在侧栏「任务」上始终有计数）。
- 「今天」和「任务」两个一级入口**都保留**：它们心智不同（时间焦点 vs 全量库），重复的是**内容**而非名字，所以删的是内容不是入口。
- 右侧「情境检查器」未改动：它未选中任务时显示「今日概览」，选中任务时显示详情，定位是补充信息而非任务入口。

**验证结果**：

| 验证项 | 结果 |
|---|---|
| `vite build` | ✅ 通过 |
| 无头 Chrome 冒烟（`.smoke/smoke.mjs`） | ✅ **30/30**（新增 IA1/IA2/IA3 三条锁定新架构），**0 条 JS 运行时错误** |
| 静态审计（`.smoke/audit.mjs`） | ✅ 死入口 0 / 零引用函数 0 / 零引用变量 0 / 未用图标 0 / 未用 CSS 0 |

**测试脚手架的同步修正**（重要，否则后续会被误判为回归）：任务卡片现在只在「任务」页渲染，原「D1 载入示例」「D1 清空示例」「C2 卡片弹窗」三条断言都假设默认视图（今天）有任务卡片，故新增 `goView(c, id)` helper（点侧栏入口，走用户真实路径），在断言前先切到「任务」页。

---

### 2026-09-15 · 右侧「今日概览」面板 → 主内容区顶部 KPI 卡片

**触发**：用户指出右侧那个「今日概览」面板本身就是多余的（它就是上一个改造里保留的"补充信息"）。要求把这四格数字做成 KPI 卡片放到顶部，面板这块就不需要了。

**落地**：

| 改动 | 说明 |
|---|---|
| 新增 `kpiStrip()` | 在主内容区**最顶部**（横幅之后、视图内容之前）铺一行 4 张卡片：待办 / 逾期 / 进行中 / 已完成 |
| `KPI_VIEWS` | 只在 `today / all / calendar / quadrant / logbook / projects` 出现；总览与报告本身是统计视图，不重复铺一层；空库时不出现（首页该是空态而不是四个 0） |
| 删除 `inspOverview()` | 连同 `.insp-metrics / .insp-metric*`、`.insp-near .in-it .in-d` 等孤儿样式一起清掉 |
| 新增 `inspHasContent()` | 仅当「项目视图落在具体项目上」或「选中了任务」时为真 |
| `renderInsp()` 重写 | 没有内容可看时**整块隐藏**（清空 + `display:none`），主内容区拿到全宽；有内容时按项目详情 / 任务详情渲染 |
| 顶栏按钮跟随显隐 | 「收起 / 展开面板」在面板无内容时 `display:none`；可见性在 `renderMain()` 与 `renderInsp()` 两处对齐 —— `edit` 只调后者（为保住列表滚动位置），必须同步 |
| `edit` 先清 `collapsed` | 否则上次收起过面板会导致"点了任务没反应" |

**有意保留**：右侧面板不是被删掉，而是**从"常驻概览"改成"按需详情"**。选中任务 → 任务详情（状态 / 优先级 / 进度 / 最近跟进 / 编辑·完成·删除）；落在具体项目 → 项目详情（摘要 / 核心目标 / 进行中 / 待办）。这两块内容在别处没有替代入口，直接删除会丢功能。

**验证结果**：

| 验证项 | 结果 |
|---|---|
| `vite build` | ✅ 通过 |
| 无头 Chrome 冒烟（`.smoke/smoke.mjs`） | ✅ **32/32**（新增 IA4 顶部 KPI / IA5 面板按需出现与收起），0 条 JS 运行时错误 |
| 静态审计（`.smoke/audit.mjs`） | ✅ 五类全 0（501 个 CSS 类） |

---

## §10 追加：KPI 卡片可见性修复 + 钻取（2026-09-15 第二轮反馈）

**用户反馈**：「这里没有看到对应的KPI卡片」（附「今天」页截图，红框标在顶栏下方）。

**根因**：`kpiStrip()` 首行 `if(!db.tasks.length)return ""` —— §9 里我自己定的「空库不显示四个 0」被用户当成了 bug。用户的库是空的，所以整条被判空。位置与视图白名单都没问题。

**结论**：四条 0 并不是噪音，它是版式的一部分，也是"任务量第一眼读数"。**改为始终显示**。

### 改动清单（`src/index.html`）

| 改动 | 说明 |
|---|---|
| `kpiStrip()` 去掉空库守卫 | 空库也渲染 4 张卡片；计数为 0 的卡片 `disabled` 且**不带状态色**（颜色只用来提示"这里有东西要看"，0 就该是安静的灰） |
| `.kpi` 由 `div` 改为 `button` | 补 `font:inherit / text-align:left / width:100%`；新增 `:hover`（`--line2` 边框 + `--shadow-2` + 上移 1px）、`:disabled`（`opacity:.5`）、`.on`（`--pri` 边框 + `--ring`） |
| 新增 `ui.kf` | 钻取口径：`all` / `todo` / `od` / `doing` / `done`，配 `KF_LABEL` 中文名 |
| 新增 `kpiFiltered()` | 叠加在 `filtered()` 之上；`viewAll()` 列表路径与 `boardTasks()` 看板路径**共用**，看板同样吃这个口径 |
| 新增 `kfChip()` | 任务页筛选条里一枚可关闭的芯片；`filterBar()` 与 `boardFilterBar()` 共用；看板「清空筛选」的显隐条件也带上 `ui.kf` |
| `kpi` 动作 | 点卡片 → 钻到「任务」页并切回列表形态；再点同一张 → 取消；`data-id="all"` 只清口径 |
| `bprojClear` | 一并重置 `ui.kf` |
| `viewAll()` 重写 | 钻到「已完成」时已完成区当主角（不再出现「进行中」空块）；钻到待办/逾期/进行中时只列未完成，标题换成对应口径名 |
| 新增 `activeFilterNames()` + `filteredEmpty()` | 空结果时明说「当前生效的筛选：逾期、优先级 高」，而不是笼统一句"清掉筛选" |

**为什么 `ui.kf` 只作用于「任务」页**：钻取是"从概览进入明细"的单向动作，若做成全局筛选，用户切到日历/四象限后会被一个看不见的条件筛空。收敛在任务页 + 页内可见芯片，语义闭环。

### 验证结果

| 验证项 | 结果 |
|---|---|
| `vite build` | ✅ 通过 |
| 无头 Chrome 冒烟（`.smoke/smoke.mjs`） | ✅ **34/34**（新增 KPI0 空库也显示 4 张且全置灰；KPI1 点待办 → 卡片数=待办数、已完成卡片=0、芯片出现；点芯片 → 恢复），0 条 JS 运行时错误 |
| 静态审计（`.smoke/audit.mjs`） | ✅ 五类全 0（501 个 CSS 类） |
| 人工核对截图（新增 `.smoke/shot.mjs`） | ✅ 5 张：空库今天页（4 个灰 0）/ 有数据今天页（4·2·1·2，逾期红·进行中蓝·已完成绿）/ 任务列表 / 逾期钻取（芯片 + 「逾期 · 2 项」）/ 看板（三列 1·1·0 = 逾期总数，筛选条同样有芯片） |

**工程教训**：同一条消息里对**同一个文件**并行发两个 Edit，后写覆盖先写，前一处改动会静默丢失（`filterBar()` 的改动就是这样被吞掉的，工具仍回 success）。发现方式是 grep 构建产物确认新代码真的进包。**规则：同文件多处修改串行发 + 改完校验产物。**

---

## §11 追加：空状态里的重复按钮（2026-09-15 第三轮反馈）

**用户反馈**：红框标出①「今天」页顶部的快速添加行 ②其下方空状态里的「＋ 加一条任务」按钮 —— 「这里两个功能是不是重复了」。

**结论：是重复，而且点下去什么也没发生。** 两处都是"添加任务"，而 `newTask` 动作的实现是 `inlineCapture()` —— 只是把光标送回**上面那一行**已经存在的输入框。所以那个按钮既重复又空转。

### 由此修正 D2 的原则（重要）

原 D2 写的是「每个空状态都包含 ①这里放什么 ②为什么有用 ③**一个明确动作**」，当时的验收口径是"`.estate` 必须含主行动按钮"。**这条口径过粗**，被机械执行就会造出这种空转按钮。

修正为：

> 空状态负责回答 ①这里放什么 ②为什么有用 ③**现在能做什么**。
> 但"能做什么"**优先指向页面上已有的入口**（用一句话说明"写在上面那个输入框里"），
> **只有当这个动作在页面别处没有入口时，才放一个按钮。**

### 改动清单（`src/index.html`）

| 位置 | 处理 |
|---|---|
| `viewToday()` 空状态 | 删掉 `acts:[{label:"加一条任务"…}]`；文案改为三段均衡断行：「清爽。/ 要不要把这周的目标拆成几个任务？/ 写在上面的输入框里，回车即可。」 |
| `filteredEmpty()` | **只在空库时**删掉「写一条任务」按钮；「清空筛选」保留 —— 那个动作在页面上别无入口 |
| `demoHintHtml()` | 删掉"直接在最上面的输入框写一条任务试试"半句（与空状态、输入框自身同屏重复三遍），只保留独有的「载入示例数据」入口 |
| `viewCalendar()` 的「给任务设个日期」 | **保留** —— 日历页没有快速添加行，这个按钮确实是唯一入口 |

### 验证结果

| 验证项 | 结果 |
|---|---|
| `vite build` | ✅ 通过 |
| 无头 Chrome 冒烟（`.smoke/smoke.mjs`） | ✅ **35/35**，0 条 JS 运行时错误 |
| D2（改写） | 今天页空状态有标题 + 说明、**无**与输入框重复的按钮、且上方确实存在 `.qadd` 输入框行 |
| D2b（新增） | 反向检查：总览页「还没有项目」空状态**仍有**「新建项目」按钮 —— 证明不是把全站按钮一刀切删掉 |
| 静态审计（`.smoke/audit.mjs`） | ✅ 五类全 0（501 个 CSS 类） |
| 人工核对截图 | ✅ `today-empty.png`：按钮已消失、文案三行均衡、底部提示只剩「载入示例数据」 |

**注**：D2b 最初写成"项目页空状态仍有新建项目"，跑出来是 false —— 查证后发现那个 `还没有项目` 空状态其实在 `viewDashboard()`（总览页）里；项目页的「新建项目」是 `.pbar` 里的**常驻**按钮，不是空状态按钮。断言已改到总览页。**教训：写断言前先确认目标 DOM 到底由哪个函数渲染。**

---

## §12 追加：快速添加行删除，新增改为「按钮 → 弹窗」（2026-09-15 第四轮反馈）

**用户反馈**：红框标出「今天」页顶那整行快速添加（未归类 / 输入框 / 优先级 / 日期 / 客户 / ＋添加），并指出它与右上角「新建」按钮重复 —— 「这个和上面的新建按钮也重复了，下面的那个不要了，保留新建按钮，并且点击后弹出弹窗进行编辑」。

**结论：重复，且这次是整块重复。** 那行的存在理由（`addTask()` 从 `#qt` 读值 → 落库）与「新建」按钮（`newTask → inlineCapture()`，把光标送进 `#qt`）本就是同一件事的两半拼起来的：按钮是那行输入框的聚焦器，输入框是按钮的落点。同屏并列，确实是两套入口。

### 改动清单（`src/index.html`）

| 改动 | 说明 |
|---|---|
| 删除 `quickAdd()` | 那整行 UI；「今天」与「任务」两页都不再渲染 |
| 删除 `addTask()` 与 `add` 动作分支 | 只服务于 `#qt` 的提交逻辑；`#qt` 已不存在 |
| 删除 `qcliHtml()` / `qproj` 变更分支 / `#qt` 的 Enter·Esc 处理器 | 全部是那行的配套 |
| **`openTask()` 支持 `__new__`** | 新增 `draftTask()` 造一个「还没进 db 的虚拟任务」，新建与编辑**共用同一张表单** |
| 新建时隐藏 4 块 | 关联笔记 / 子任务进度 / 跟进记录 / 删除按钮 —— 它们都需要任务已经存在（各自的数据挂在 `t.id` 上） |
| `inlineCapture()` → `openTask("__new__")` | 顶栏「新建」、托盘「新建」、自定义快捷键三处共用此函数，一处改全线生效 |
| `saveTask` 新增 `__new__` 分支 | 组装新任务 → `db.prefs` 记住项目/优先级 → `unshift` → 保存 |
| 标题框回车即保存 | 保留「回车即可」的手感，不用挪手去点保存 |
| 掩膜点击：新建可关闭 | `maskClose` 放行 `__new__`（新建无改动可丢）；编辑仍不可（防误丢） |
| 文案改写 | 今天空状态「写在上面的输入框里」→「点右上角「新建」写下第一件」；空库空状态、上手三步第 1 步同步改指「新建」 |
| 删除 `.qadd*` / `.qcli*` / `.go` 全套样式 | 含 3 处媒体查询片段与 `:focus-visible` 选择器里的 `.go` |

**内联语法（`!高` / `@标签` / `明天` / `*重要`）没有丢**：`parseTitle()` 仍在新建提交时执行，只是载体从「那行输入框」换成「弹窗标题框」。⌘K 命令面板的「新建任务：<文本>」也照旧支持。

### 验证结果

| 验证项 | 结果 |
|---|---|
| `vite build` | ✅ 通过 |
| 无头 Chrome 冒烟（`.smoke/smoke.mjs`） | ✅ **35/35**，0 条 JS 运行时错误 |
| C1（改写） | 点「新建」→ 弹窗标题为「新建任务」、`#mt` 已聚焦、无删除按钮、无命令面板、**页面无 `.qadd`** |
| D3（改写） | 在弹窗标题里敲回车 → 弹窗关闭 + 列表出现该任务 + 「上手三步」引导条出现 |
| C2（改写） | 新建弹窗里客户由项目推导、默认只回显（`#mc` 不可见）；且页面已无快速添加行 |
| D2 / IA2 / IA4（改写） | 反向锁死「`.qadd` 已不存在」，并把 IA2/IA4 的位置断言锚点从 `.qadd` 换成 `.fbar` / `.today-box` |
| 静态审计（`.smoke/audit.mjs`） | ✅ 五类全 0（495 个 CSS 类） |
| 人工核对截图 | ✅ `today-empty.png`（那行已消失、文案指向右上角）、`new-task-modal.png`（标题/日期/项目/客户回显/更多选项/取消·保存） |

**注（测试脚手架）**：`.smoke/shot.mjs` 增加第 6 张图 `new-task-modal.png`；浏览器直开 `dist/` 时顶部会出现「保存暂时失败，正在重试…」横幅 —— 这是**改造前就存在**的（浏览器里没有 Tauri 的 `invoke`，`save_store` 必然失败），已用旧提交 `1011bb3` 单独构建复现确认，不是本轮引入。

---

## §13 追加：弹窗宽度比例失调（2026-09-15 第五轮反馈）

**用户反馈**：「太宽了，比例失调了」（附「新建任务」弹窗截图，红框圈出整个弹窗）。

**根因**：`openTask()`（新建/编辑任务）、`openProject()`（新建/编辑项目）、`openStages()`（管理项目阶段）三个弹窗**都没写 `max-width`**，于是继承了 `.sheet` 的默认值 **880px**。而这三个表单本体都是**两列**（`.two-col` = `1fr 1fr`）甚至单列：

- 880px 下扣除内边距 26×2、列间距 18，**每列约 360px**
- 结果：任务标题单行输入框 745px 宽、「选择截止日期」与「未归类」下拉各 360px，内容只占三四行，宽高比约 2.3:1 —— 就是用户说的"比例失调"

对比同文件里其它弹窗的既有取值：560（插入链接 / 管理分类 / 导出 / 导入）、600、620、720、760、840、920 —— **880 只被这三个没写的弹窗用到**，说明它是个"没被设计过的兜底值"。

### 改动清单（`src/index.html`）

| 改动 | 说明 |
|---|---|
| `.sheet` 默认 `max-width:880px` → **`560px`** | 默认值应当代表最常见形态（两列表单）。需要宽的弹窗本来就已经各自显式覆盖 |
| 新增 `.two-col.auto-right{grid-template-columns:1fr auto}` | 左栏自适应 + 右栏按内容宽度；含 `@media (max-width:680px)` 回落单列 |
| 项目弹窗「客户 \| 颜色」改用 `two-col auto-right` | 顺带修掉收窄后**新暴露**的问题：色板 8 色需 287px（28px 圆点 + 9px 间距），按 `1fr 1fr` 均分只有 245px，会折成 6+2 两行 |

**为什么改默认值而不是给三个弹窗各写一行**：880px 这个"兜底值"本身就是错的，任何后续新增的弹窗都会踩同一个坑。改默认值 + 需要宽者显式覆盖，才是自洽的规则。

### 验证结果

| 验证项 | 结果 |
|---|---|
| `vite build` | ✅ 通过 |
| 无头 Chrome 冒烟（`.smoke/smoke.mjs`） | ✅ **36/36**，0 条 JS 运行时错误 |
| UI1（新增） | 弹窗 `getComputedStyle(sheet).maxWidth === '560px'`、实际宽 ≤ 620、标题输入框 > 200、弹窗宽 / 视口宽 < 0.7 |
| 静态审计（`.smoke/audit.mjs`） | ✅ 五类全 0（496 个 CSS 类） |
| 人工核对截图 | ✅ `new-task-modal.png`（560px：标题/截止日期·项目两列/客户/更多选项/取消·保存）、`project-modal.png`（客户与色板同行、8 色一行排开）、`stages-modal.png`（4 行阶段名+计入已落地+删除，560px 富余）、`today-back.png`（关闭弹窗回到今天，无残留） |

**量尺寸的方法**：`.smoke/shot.mjs` 用 `--window-size=1440,900`，产出 PNG 原生 **1418x802**。对话里显示的图会被缩放，**必须按 PNG 原生像素换算**（本轮弹窗实测 560px = 原生像素，占视口 39.5%）。

---

## §14 追加：弹窗内按 Esc 关日期面板，却把整个弹窗也关了（2026-09-15 第六轮反馈）

**用户反馈**：「选择日期这里按 esc 直接关闭弹窗了，而不是返回打开日期之前」。

**根因：两个 `document` 级 keydown 监听器抢同一次按键，谁也没给谁让路。**

```
document.addEventListener("keydown",function(e){if(DP&&e.key==="Escape")closeDatePicker()});      // 注册在前
document.addEventListener("keydown",function(e){if(USOPEN&&e.key==="Escape")closeUse()});        // 注册在前
document.addEventListener("keydown",function(e){ ... if(e.key==="Escape"){closeModal();...} });  // 主处理器，注册在后
```

- 同一元素上的多个监听器**互不阻塞**，`return` 只结束自己那一个 —— 所以一次 Esc 会同时命中「关面板」和「关弹窗」
- 而且执行顺序是注册顺序：日期面板的监听器**先**把 `DP` 清成 `null`，等主处理器跑起来时「浮层还开着吗」的判断**恒为假**
- 第一次尝试修就是在主处理器里加 `if(DP||USOPEN)return`，实测仍然 FAIL（`.smoke/smoke.mjs` UI2 报 `1次Esc后面板=false(弹窗在=false)`）—— 因为守卫读到的是被前一个监听器清空后的值

**这是第五轮 §13 里 `Escape` 直觉的延伸：Esc 应当逐层退，一次只关一层。**

### 改动清单（`src/index.html`）

| 改动 | 说明 |
|---|---|
| **删除** `#dp` 的独立 keydown 监听器 | 它的 Esc 职责收回全局处理器 |
| **删除** `#uselpop` 的独立 keydown 监听器 | 同上 |
| 全局处理器新增「浮层逐层退」分支 | 按层级顺序：`DP` → `USOPEN` → `closeModal()`；放在子任务行内编辑、命令面板分支之后 |

**为什么收敛而不是加守卫**：把「关自己」留在浮层里、再在主处理器里判断"别人关没关"是不可能可靠的 —— 状态已被清掉。**一个按键阶段只能有一个权威处理器**，层级顺序显式写在那一处，才看得懂也测得准。

### 验证结果

| 验证项 | 结果 |
|---|---|
| `vite build` | ✅ 通过 |
| 无头 Chrome 冒烟（`.smoke/smoke.mjs`） | ✅ **37/37**，0 条 JS 运行时错误 |
| UI2（新增） | ① 点开日期面板 → 1 次 Esc：面板 `open=false` 且**弹窗仍在**；② 点开项目下拉 → 1 次 Esc：下拉 `open=false` 且**弹窗仍在**；③ 浮层都收了以后再按 Esc → 弹窗才关闭；④ 重新打开弹窗成功（保证后续用例可继续） |
| 静态审计（`.smoke/audit.mjs`） | ✅ 五类全 0（496 个 CSS 类） |
| 人工核对截图 | ✅ `date-picker.png`（弹窗内日期面板正常）、`after-esc-date.png`（Esc 后面板消失、弹窗保留、标题框仍是焦点） |

### 遗留（同类问题，未修）

从任务弹窗里点「＋新建项目 / 新建客户 / 关联笔记」开的**嵌套弹窗**，Esc / 取消走 `closeModal()` → 清空整个 `#modal`，写到一半的任务一起丢。同样是"Esc 应该退回上一层"。

机制其实已经有了：`snapshotTaskDraft()` 把表单存进 `ui.taskDraft`，`openTask(taskId)` 会把它读回来 —— **保存路径就是这么回到任务弹窗的**（`saveProject` 里的 `openTask(_tp)`）。

但不能简单改 `closeModal()`：笔记选择器有自己的一套回退（`noteReturnTo` + `renderAfterNoteChange`，`noteReturnTo==="taskNotes"` 时要回「任务笔记」弹窗而不是任务弹窗），一刀切会带坏它。要做需要给嵌套弹窗单独加一层「返回栈」。**已征询用户是否要修。**

---

## §15 追加：「设置」混在顶栏动作组里，与其他按钮不在一个维度（2026-09-15 第七轮反馈）

**用户反馈**：「这个设置放在这里很割裂，和其他的几个按钮并不在一个维度上」（截图圈出顶栏的「设置」）。

**诊断：顶栏动作组的三个按钮各自属于不同的层，设置被降格成了「又一个按钮」。**

| 按钮 | 作用域 | 语义 |
|---|---|---|
| 导出 / 导入 | 当前这份数据 | 对数据本身做一个动作 |
| ＋新建 | 当前这一屏 | 当前视图的主操作（primary） |
| **设置** | **整个应用** | **配置应用本身（AI 服务 / 启动项 / 数据目录 / 快捷键）** |

一排同款 `.tbtn` 把三者摆成同等权重，等于说「设置」和「新建一条任务」是同一层级的事。而它真正的层级比顶栏高一层 —— 它不随视图变化，改的是应用本身。

**方案：设置收进侧栏底部，成为常驻的「应用级」入口。**

这不是新造位置，而是把入口放回它本来该在的层：Things 3、Notion、VS Code 都把设置放在侧栏/窗口的底角 —— 与「内容导航」物理分开，且不随内容滚动消失。

### 改动清单（`src/index.html`）

| 改动 | 说明 |
|---|---|
| **侧栏改为两段式** | `.sidebar` 由 `overflow:auto` 的单容器改成 `display:flex;flex-direction:column`；新增 `.side-scroll`（`flex:1;overflow:auto`，承载原有全部内容导航）+ `.side-foot`（`flex:none`，钉在底部） |
| `renderSide()` 输出结构调整 | 内容包进 `.side-scroll`，末尾追加 `.side-foot` 里的设置入口（`data-act="settings"` 复用原有派发，无新逻辑） |
| **顶栏删除「设置」按钮** | `.ch-acts` 恢复为 导出 / 导入 / 新建 三个动作 |
| **移动端兜底入口** | ≤768px 时 `.sidebar{display:none}`，设置会跟着消失。顶栏保留一个 `.tbtn.mo-only`（桌面端 `display:none`，移动端 `display:inline-flex`）的齿轮图标按钮 |
| 图标换成语义正确的齿轮 | 原顶栏用的是「太阳」形状（`circle r=3` + 八条射线），看着像亮度调节。新增 `IC.gear`（Feather settings 齿轮），两处入口共用 |
| 底部入口带快捷键提示 | 右侧显示 `⌘,` / `Ctrl+,` —— 这个快捷键（`index.html` 全局 keydown `e.key===","` 分支）一直存在，但原顶栏按钮没暴露它，属于已有能力没被发现 |

**为什么不把「导出 / 导入」也一起收进设置**：它们是「对当前数据做点什么」，属于数据操作而非应用配置；且顶栏的「建议点右上角导出一份 JSON 备份」提示依赖它留在显眼处。若后续觉得也该收，正确做法是并进设置面板的「数据存储位置」区（那里已经在管数据目录），而不是再挪一次位置。

### 验证结果

| 验证项 | 结果 |
|---|---|
| `vite build` | ✅ 通过 |
| 无头 Chrome 冒烟（`.smoke/smoke.mjs`） | ✅ **38/38**，0 条 JS 运行时错误 |
| IA6（新增） | ① 侧栏底部存在**可见**的设置入口；② 顶栏 `.ch-acts` 里 `offsetParent!==null` 的设置按钮数为 **0**；③ 顶栏可见按钮文案里**不含**「设置」；④ 点侧栏入口真的打开设置面板（标题 = `设置`）；⑤ Esc 能关掉它 |
| 断点回归（`.smoke/responsive.mjs`，**本轮新增**） | ✅ **7/7** —— 桌面 1440：侧栏可见 + 底部设置可见 + 顶栏设置不可见；移动 420：侧栏整条隐藏 + 顶栏图标可见 + 点它真能打开面板 |
| 静态审计（`.smoke/audit.mjs`） | ✅ 其余五类全 0（499 个 CSS 类、40 个图标，新增的 `.side-scroll/.side-foot/.mo-only` 与 `IC.gear` 均被引用） |
| 人工核对截图 | ✅ `sidebar-scrolled.png`（侧栏内容滚到底，设置仍钉在底部）、`settings-modal.png`（点侧栏设置打开面板）；`today-empty.png` 显示顶栏已是 导出/导入/新建 三连 |

### 为什么新增 `responsive.mjs` 而不是塞进 smoke

「同一功能两个入口、靠断点切换」是最容易悄悄失效的写法 —— 两边都藏起来 = 功能彻底消失，而任一单宽度的测试都发现不了。这类**断点对称性**问题值得一个独立探针：它同时验宽度两端的可见性，并在移动端真点一次。smoke.mjs 固定跑桌面宽度，覆盖不到。

### 本轮的通用原则

> **信息架构上要区分「内容导航」与「应用级操作」。** 前者随视图/数据变化（今天、项目、客户、标签），后者恒定（设置、账号、帮助）。两者放同一排，用户会以为它们是同一层级的选项。
