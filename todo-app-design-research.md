# To-Do 类产品设计调研与借鉴清单

> 调研目的：为「我的任务台」（Tauri 桌面 · macOS 风格）梳理行业标杆产品的优秀设计，形成一份可审查的对照清单，明确哪些值得借鉴、如何落地、优先级如何。
> 调研时间：2026-08-10 ｜ 标杆对象：Things 3、Todoist、TickTick、OmniFocus、Apple Reminders、Microsoft To Do、Linear。

---

## 0. 我们当前已具备的能力（基线）

| 维度 | 现状 |
|---|---|
| 归类 | 项目（带颜色）、优先级 P0–P3、截止日、客户分类（设计中） |
| 任务结构 | 子任务（可拖拽排序、可就地编辑）、跟进记录 logs（增删改，卡片内嵌） |
| 视图 | 列表（今日/任务/总览/报告 tabs）、看板（待办/进行中/已完成）、报告/总览聚合 |
| 录入 | ⌘K 命令面板、tabs 行「新建任务」、卡片内嵌快捷记录 |
| macOS 适配 | 毛玻璃侧栏/顶栏、顶部 sheet 弹窗、分段控件、键盘 focus-visible 焦点环、顶栏主操作 |
| 数据 | 本地 JSON（~/Documents/我的任务台/），无云端 |

以下每个借鉴点都标注：**借鉴什么 → 如何落到我们的 app → 优先级**。

---

## 1. 组织维度：如何给任务归类（与"客户分类"强相关）

这是本次调研的核心，因为正在给项目加「客户」上层。

### 1.1 Things 3：Inbox / Today / This Evening / Areas / Projects 层级
- **设计**：收集箱（Inbox）先收容一切 → 再归到 Area（生活领域，如工作/家庭）→ Project（具体目标）。「This Evening」把下班后的个人事务与白天工作分离，心理切换更顺。
- **落地建议**：
  - 我们的「客户」= Things 的 Area（上层归类），项目 = Project（下层），结构天然契合，无需改概念。
  - 可在「今日」tab 内增加「今晚」软分区（按任务是否标了"个人/非工作"），属锦上添花。
- 优先级：**客户分类本身已规划，P0 完成即可**；「今晚」分区 P2。

### 1.2 Todoist：Projects + Labels + Filters
- **设计**：项目是归属容器；**标签（Labels）是跨项目的横向分类**（如 `#等待中` `#电话` `#客户A`），可叠加在任意项目上；**过滤器（Filters）** 用条件组合出自定义视图（"今天所有 P1" 或 "工作项目里 @等待中"）。
- **落地建议**：
  - 客户（纵向，归属）+ **标签**（横向，跨项目）形成二维归类，互补不冲突。
  - 非常适合"同一项目里不同任务分属不同客户/不同性质"的场景——这正是单纯"客户挂在项目上"覆盖不到的。
  - 过滤器可后做，但标签值得尽早引入。
- 优先级：标签 **P1**；过滤器 P2。

### 1.3 Apple Reminders：Lists + Folders + Tags + Smart Lists
- **设计**：列表（List）→ 文件夹（Folder，分组多个列表）；**标签（#）跨列表过滤**；**智能列表（Smart List）= 按日期/标签/优先级/地点自动聚合的活视图**（如"本周高优先级"）。
- **落地建议**：我们的"项目"≈ List，"客户"≈ Folder（分组），一致。Smart List 对应可考虑做"智能筛选"入口。
- 优先级：参考，不单独立项。

### 1.4 OmniFocus：Projects + Folders + Contexts/Tags + Custom Perspectives
- **设计**：Context（上下文：人/地点/状态/精力）是独立于项目的第三维；**自定义透视（Perspectives）** 是高级用户的灵魂——围绕文件夹/项目/标签/日期任意组合出专属视图。
- **落地建议**：Perspectives 太重，个人版不必做；但其"上下文/标签"思想与 1.2 一致。
- 优先级：仅参考。

> **小结（归类维度）**：客户（纵向，归属）→ 项目（纵向，下层）→ 标签（横向，跨项目）。三维最完整也最不过度。建议客户 P0、标签 P1、过滤器/智能视图 P2。

---

## 2. 任务录入体验

### 2.1 快速录入 / 全局快捷键 / Magic Plus
- Things「Quick Entry」全局快捷键随地捕获；「Magic Plus」把 + 拖到列表任意位置就地新建。
- 我们已有 ⌘K 命令面板 + tabs 行「新建任务」，**已具备快速录入骨架**。
- 可增强：命令面板支持"输入即解析"（见 2.2）；卡片 hover 时行内 + 就地新建子任务（我们已有 quickLog，可类比加行内"加子任务"）。
- 优先级：行内加子任务 **P1**；全局快捷键（Tauri global shortcut）**P2**。

### 2.2 自然语言日期解析（高价值借鉴）
- Todoist / Things / Reminders 都支持"明天下午 2 点""每周一""3 天后"直接解析为日期/重复。
- **落地建议**：在 ⌘K 输入框与新建任务框接入轻量 NL 日期解析（如解析"明天""下周一""每周一"），显著降低填日期的摩擦。
- 优先级：**P1**（体验提升明显，实现量中等）。

### 2.3 输入框智能识别
- TickTick 在输入时识别时间/优先级并设置。
- 落地建议：可复用 2.2 的解析器，在标题里识别"!p1""明天"等。
- 优先级：P2（与 2.2 合并做）。

---

## 3. 视图设计

| 视图 | 代表产品 | 我们现状 | 借鉴建议 | 优先级 |
|---|---|---|---|---|
| 列表 | 全部 | ✅ 已有 | 维持 | — |
| 看板 | Todoist/TickTick/我们 | ✅ 已有（三列） | 可加"按负责人/标签"分列 | P2 |
| **日历视图** | TickTick/Things/Fantastical | ❌ 无 | 把任务按截止日铺到月/周日历网格，直观看负载 | **P1** |
| **时间线** | TickTick | ❌ 无 | 轻量甘特，按估时显示任务条长，拖拽调期 | P2 |
| **四象限** | TickTick( Eisenhower) | ❌ 无 | 按重要/紧急分四区，聚焦要事 | P2 |
| **预测/今日** | OmniFocus Forecast | 部分（今日 tab） | 左侧 mini 日历 + 右侧当日任务，统筹日程 | P1 |

> 最值得做的是**日历视图**和**预测/今日视图**：当前"今日"只有列表，无法感知一周负载与空闲，是明显短板。

---

## 4. 跟进、回顾与日志

### 4.1 跟进记录（我们已有，可增强）
- 我们已有卡片内嵌 logs（增删改 + 最新预览 + 右侧浮卡）。
- **借鉴 TickTick/Things 的"类型 + 时间"**：给每条跟进加类型标签（电话/邮件/会议/已回复），并在报告里按类型聚合。
- 优先级：类型标签 **P1**（之前提过，用户未否决）。

### 4.2 周回顾模式（强推荐借鉴）
- OmniFocus「Review」：每个项目有"下次回顾日"，到点自动进回顾列表，逐项目确认是否还相关、要增删。这是 GTD 的核心闭环。
- **落地建议**：给项目加 `nextReview` 日期，侧边栏/今日加"待回顾"区，点开逐项目过一遍（类似我们已有的项目编辑弹窗 + 一个"已回顾"标记）。
- 优先级：**P1**（大幅提升"项目不腐烂"的体验，且与客户/项目结构天然契合）。

### 4.3 完成历史 / Logbook
- Things 把已完成任务收进 Logbook 供回溯。
- 我们报告里已有完成聚合，可加"已完成时间线"方便写周报。
- 优先级：P2。

---

## 5. 提醒与时间

- **本地通知**（Tauri notification API 可行）：截止日前/逾期提醒。我们目前无提醒，是空白。
- **重复任务**：我们已有 `repeat`（每日/每周/每月），可增强为"每工作日""每 3 个周一"等。
- **地点提醒**：桌面端价值低，跳过。
- 优先级：本地通知 **P1**（与任务相关性高）；复杂重复 P2。

---

## 6. 报告与洞察

- TickTick 数据统计（完成数、专注时长、习惯）、按维度聚合。
- 我们报告/总览已有按项目聚合，**加了客户后顺带加"按客户聚合"**（在之前方案里列为可选增强，建议提升为 P1，因为客户维度的主要价值就在聚合看板）。
- 习惯打卡：个人版价值有限，P3。
- 优先级：**按客户聚合 P1**；统计图表优化 P2。

---

## 7. 键盘与效率

- **全局/上下文快捷键**：Linear/Things 的 ⌘N 新建、⌘K 跳转、`.` 完成、Type Travel 输入即跳转。
- 我们已有 ⌘K、focus-visible 焦点环、回车提交等，**键盘可达性已较好**。
- 可补：任务卡 `⌘↵` 完成、`e` 编辑、`/` 聚焦搜索。
- 优先级：快捷键补全 **P2**。

---

## 8. macOS 设计语言（我们已做 + 可继续）

已符合：毛玻璃侧栏/顶栏、顶部 sheet、分段控件、焦点环、SF 字体、覆盖滚动条、红绿灯安全区。
可继续（之前审核清单剩余项，用户已全选未做细分）：
- 列表/看板分段控件统一（已做）
- 圆角统一 8px、卡片 hover（已做）
- 顶栏主操作 + 强调色微调（已做）
- 子任务铅笔 hover、命令面板/空状态视觉对齐（已做）
- **仍可打磨**：完成动效（勾选时的微动画）、空状态插画、深浅色对比度、列表行 hover 更克制。

---

## 9. 落地优先级总表（供审查拍板）

### P0（客户分类进行中，先收口）
- 客户作为项目上层 + 独立管理（侧边栏客户区、项目挂 clientId、筛选）

### P1（高价值、体验质变）
- 标签系统（横向跨项目分类，补客户挂项目覆盖不到的场景）
- 自然语言日期/优先级解析（⌘K 与新建框）
- 日历视图（周/月网格看负载）
- 预测/今日视图（mini 日历 + 当日任务）
- 周回顾模式（项目 nextReview + 待回顾区）
- 本地通知提醒（截止/逾期）
- 报告按客户聚合
- 跟进记录类型标签（电话/邮件/会议/已回复）

### P2（增强项）
- 行内加子任务、全局快捷键
- 看板按标签/负责人分列、时间线、四象限
- 过滤器/智能视图
- 完成历史时间线、统计图表优化
- 快捷键补全（⌘↵ 完成等）

### P3（可选）
- 习惯打卡、复杂重复规则、地点提醒

---

## 10. 我的建议路线
1. 先收口 **P0 客户分类**（当前进行中）。
2. 紧接着做 **标签 + 自然语言日期 + 报告按客户聚合**——三者都围绕"归类与录入摩擦"，与本次客户分类一脉相承，性价比最高。
3. 视图层优先 **日历视图 / 预测今日**（解决"只看到列表、看不到负载"的短板）。
4. 闭环层做 **周回顾 + 本地通知**（让系统主动提醒，而不只是被动记录）。

---

## 11. 参考资料
- Things 3 — Cultured Code（Apple Design Award，GTD 标杆）
- Todoist — Features（自然语言、Labels、Filters、Karma）
- TickTick — Features（日历/时间线/四象限/习惯/番茄）
- OmniFocus 4 — Perspectives & Review（自定义透视、周回顾）
- Apple Reminders — Smart Lists / Tags / Folders（系统原生范式）
- Microsoft To Do — My Day / 智能建议
- Linear — 键盘驱动、Issue 状态/优先级（效率范式参考）
