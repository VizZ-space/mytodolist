# 界面规范 · 弹窗（ui-spec.md）

> 适用范围：`my-task-desktop/src/index.html` 里所有渲染进 `#modal` 的弹窗。
> 本文是**唯一真相**：`.smoke/modal-audit.mjs` 把下面每一条都写成了断言，改代码后跑它就行。
>
> 定位说明：`ux-simplification-plan.md` 记录的是"为什么要改"（决策过程），本文记录的是"改成什么样"（标准本身）。

---

## 0. 一句话

**宽度只能从 5 档命名阶梯里选；标题一律 `h3`；页脚一律 `sheet-act`；动作按钮只有 3 个变体且几何写在变体上；红色只给会丢数据的动作。**

---

## 1. 尺寸阶梯

一档 = **一行能并排放几个 240px 的标准字段列**。可用宽 = 档位宽 − 52（左右 padding 各 26）。

| 档位类 | 宽度 | 可用宽 | 能放几列 | 什么时候用 | 当前使用者 |
|---|---|---|---|---|---|
| `.sheet.sm` | 480 | 428 | 1 | 一句话确认、单字段 | 确认框、新建笔记标签 |
| （不写类）`md` | **560** | 508 | **2** | **默认**：普通表单 | 任务、项目、管理阶段、管理分类、导出备份、导入恢复、已导出 MD、关于 |
| `.sheet.lg` | 720 | 668 | 2 列放宽 | 需要横向空间、带列表/预览 | 关联笔记到任务、任务笔记、引用另一篇笔记 |
| `.sheet.xl` | 880 | 828 | 3 | 多列配置表单 | 客户、标签、智能列表 |
| `.sheet.xxl` | 1160 | 1108 | 4 / 双栏 | 双栏大面板 | 设置 |

**为什么默认是 560**：560 − 52 = 508 = 2 × 240 + 28（间距）—— 正好两列。给到 880 时一栏有 360px，日期选择器和下拉会被拉成极瘦长的条形，比例失调（2026-09-15 用户实测反馈）。

### 硬规则

- **不要在 `.sheet` 上写 `style="max-width:..."`**。需要新宽度就讨论是否需要新增档位，而不是就地改数字。
- 新增档位必须同时改三处：`ui-spec.md`（本节表）、`src/index.html` 的 `:root --sheet-*` 与 `.sheet.*`、`.smoke/modal-audit.mjs` 的 `LADDER`。

---

## 2. 结构

```html
<div class="mask" data-act="maskClose">
  <div class="sheet [sm|lg|xl|xxl]" data-sheet="1">
    <h3>标题</h3>                      <!-- 必需 -->
    … 正文：.fld / .two-col / 列表 …    <!-- 自由 -->
    <div class="sheet-act">…</div>      <!-- 必需（设置面板用 .set-foot） -->
  </div>
</div>
```

| 元素 | 规则 |
|---|---|
| `.mask` | 恒带 `data-act="maskClose"`（点遮罩关闭）；遮罩本身内边距 `7vh 16px 16px` |
| `data-sheet="1"` | 标记"这是一个弹窗"。历史上一直有，现在由审计脚本用来识别 |
| `<h3>` | **每个弹窗必须有且只有一个标题**。字号 16px、下边距 14px |
| 正文 | 需要两列就用 `.two-col`（`1fr 1fr`）；左右宽度需求不对称用 `.two-col.auto-right` |
| 页脚 | `.sheet-act` = 整行等宽按钮；设置面板用 `.set-foot`（多键工具条，多一段统计文案） |

---

## 3. 排版

| 用途 | 规格 |
|---|---|
| 弹窗标题 | `h3` 16px |
| 正文说明（标题下的一句话） | 13–14px + `color:var(--muted)` + `line-height:1.6` |
| 字段标签 | `.fld label` 12.5px + `var(--muted)` + 下边距 5px |
| 字段纵向间距 | `.fld` 下边距 12px |
| 弹窗内边距 | `22px 26px 24px`（上下与左右不同，别改） |
| 最大高度 | `88dvh`，超出内部滚动 |

> 次级说明文字的 12.5 / 13 / 13.5 / 14 四种字号在设置面板里确实混用了（`.muted` 内联样式）。属于已知的收敛空间，但**弹窗结构本身**（标题 / 字段 / 页脚）已统一。

---

## 4. 按钮

### 4.1 三个变体（全项目只有这三个）

| 类 | 外观 | 用途 |
|---|---|---|
| `.btn-ghost` | 白底 + 细描边 | 取消、关闭，以及任何"不推进流程"的动作 |
| `.btn-pri` | 品牌蓝实心 | 提交 / 保存 / 确定 —— 推进流程 |
| `.btn-danger` | 浅红底 + 红字 | **只给会丢数据的动作**：删除 / 清空 / 覆盖 |

**几何必须写在变体上**，任何位置直接用即尺寸一致：

```
height 38px · padding 0 16px · border-radius var(--r-sm) · font-size 14px · font-weight 600
```

历史坑：几何原先只写在 `.sheet-act button` 里，于是 `.set-foot` 的按钮和弹窗正文里的"删除…"按钮一用变体就退化成**没有内边距的裸按钮**，只好各自 inline 抄一份尺寸 —— 而抄出来的还互不相同（`height:40px / radius:10px / font-size:14px` vs 页脚的 `43px / 11px / 14.5px`）。

### 4.2 三档尺寸（按所在位置，不是按按钮重要性）

| 位置 | 规格 |
|---|---|
| 弹窗正文里的整行操作（如"删除这个任务"） | `.btn-wide`：`width:100% + height:43px + margin-top:10px` |
| 弹窗页脚 `.sheet-act` | `flex:1` 等宽 + `height:43px + font-size:14.5px` |
| 设置面板 `.set-foot` 多键工具条 | 用变体默认的 38px（一排三个按钮，不适合再放大） |

### 4.3 页脚两种模式与顺序

| 模式 | 顺序 | 例 |
|---|---|---|
| **表单页脚**（内容是可编辑表单） | `[取消(ghost)] [主操作(pri)]` | 任务 / 项目 / 客户 / 导入恢复 |
| **收尾页脚**（内容本身是操作对象：列表、预览） | `[动作…(ghost)] [关闭或完成(pri)]` | 任务笔记 / 导出备份 / 管理分类 |

**共同规则：最右那个按钮必须是实心色（`btn-pri` 或 `btn-danger`）** —— 它要么是主操作，要么是"结束这一屏"。`btn-ghost` 永远在它左边。

> 这条规则是审计会拦的：曾经「关联笔记到任务」的顺序是 `[取消关联][新建并关联(pri)][关闭(ghost)]`，主操作被夹在中间、收尾按钮反而在最右，已修正为 `[关闭][取消关联][新建并关联(pri)]`。

### 4.4 危险色规则

**红色属于「会丢数据的动作」，不属于「确认」这个动作本身。**

`confirmBox(msg, act, opt)`：

| 参数 | 说明 |
|---|---|
| `opt.danger` | `true` → 用 `btn-danger`；否则 `btn-pri` |
| `opt.ok` | 按钮文案，默认「确定」。破坏性动作应写成具体动词 |

| 调用点 | danger | 文案 |
|---|---|---|
| 删除任务 / 项目 / 笔记 | ✅ | 删除 |
| 清空全部数据 | ✅ | 清空 |
| 覆盖恢复（导入备份 / 恢复示例数据） | ✅ | 覆盖恢复 |
| 批量顺延逾期 / 批量标记完成 / 载入示例数据 / 创建并打开笔记 / 顺延主任务截止日 / 重启 | ❌ | 确定 / 重启 |

历史坑：`confirmBox` 早前**无条件**把确认按钮画成红色，于是"载入示例数据"这种纯增益操作也是红按钮（用户截图反馈的直接来源）。

---

## 5. 弹窗清单（18 个）

| 档位 | 弹窗 |
|---|---|
| sm 480 | 确认框；新建笔记标签 |
| md 560 | 新建/编辑任务；新建/编辑项目；管理项目阶段；管理分类；导出备份；导入恢复；已导出 Markdown；关于 |
| lg 720 | 关联笔记到任务（子任务）；任务笔记；引用另一篇笔记 |
| xl 880 | 新建/编辑客户；新建/编辑标签；新建/编辑智能列表 |
| xxl 1160 | 设置面板 |

> 「关于」面板只能由原生菜单 `routeMenu("about")` 打开，浏览器里不可达 —— 它由静态脚本 `.smoke/modal-inventory.mjs` 覆盖，其余 17 个由运行时脚本覆盖。

---

## 6. 怎么保证不跑偏

> `.smoke/` 目录未入库（见 `.gitignore`），下面是**本机工具**。换机器需要从零重建，重建时按本节断言清单写即可。

```bash
cd my-task-desktop
npm run build:front          # 必须先构建，以下脚本测的都是 dist/
node .smoke/modal-audit.mjs  # 运行时：真打开 21 个弹窗，量计算样式逐条对规范
node .smoke/modal-inventory.mjs  # 静态：扫全部 18 个模板（含浏览器不可达的）
node .smoke/smoke.mjs        # 功能冒烟 38 项
node .smoke/audit.mjs        # 死代码 / 未用 CSS 审计
node .smoke/responsive.mjs   # 断点回归
```

`modal-audit.mjs` 逐项断言：① 宽度在阶梯上且无裸 `max-width` ② 有 `h3` ③ 有页脚 ④ 页脚按钮只用 3 变体 ⑤ 页脚按钮等高（`sheet-act` 还要求等宽）⑥ 动作按钮不再用 inline style 写几何 ⑦ 页脚最右是实心色 ⑧ 危险色语义正确（非破坏性确认框必须是蓝的）。

---

## 7. 改弹窗时的检查清单

- [ ] 宽度选了哪一档？凭什么（一行要放几列）？
- [ ] 有 `<h3>` 吗？有页脚吗？
- [ ] 页脚最右是不是实心色？`btn-ghost` 有没有跑到它右边？
- [ ] 这个动作会丢数据吗？会 → `btn-danger` + 动词文案
- [ ] 按钮有没有写 inline `height/width`？（写 `.btn-wide` 或变体默认尺寸）
- [ ] 跑 `modal-audit.mjs` + `smoke.mjs`
