# 我的任务台 · 交付概览

## 完成了什么
为用户的 MacBook 搭建了一个个人任务管理工作台，核心满足「任务可拆分子任务」+「任务可关联项目」两个需求，同时加入了今天要处理和周报汇总模块。

## 交付物
- `我的任务台.html`：单文件全内联工作台，手机/电脑都能打开
- `我的任务台.app`：macOS 应用包，双击启动（优先 Chrome 应用模式）
- `使用说明.txt`：Mac App 安装、Safari 添加到程序坞、数据备份说明

## 核心模块（4 个）
1. **项目**：颜色标记、任务数、完成率进度环
2. **任务 + 子任务**：优先级 P0/P1/P2、截止日、备注；子任务无限追加、可拖拽排序、勾选回算进度
3. **今天要处理**：逾期项标红并显示逾期天数、今天截止、3 天内到期提醒，支持一键「顺延今天/明天」
4. **周报自动汇总**：本周完成统计、每日完成柱状图、一键生成可复制周报文本

## 技术规范
- 单文件 HTML，零外部框架 / CDN / 字体 / 图表库
- 全部数据存在浏览器 localStorage，刷新不丢
- 首屏提供「导出 JSON 备份」「导入 JSON 恢复」，清空需二次确认
- 预置 5 条示例数据（含 1 条逾期）
- PC 三栏布局，移动端自动切换底部 Tab + 单列
- 快捷键 ⌘K 新建、⌘F 搜索、⌘S 导出备份

## 注意事项
- .app 首次打开可能触发 macOS 安全提示，需在「系统设置 → 隐私与安全性」点击「仍要打开」
- 推荐 macOS Sonoma 及以上用户使用 Safari「文件 → 添加到程序坞」，体验最接近原生 App
