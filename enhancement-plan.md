# 我的任务台 · 功能增强实施计划（基于设计调研筛选）

> 说明：本计划基于 `todo-app-design-research.md` 的调研，整合用户勾选的 **1.2 / 1.3 / 2.1 / 2.2 / 2.3 / 4.1 / 4.2 / 4.3 / 5 / 8 / 视图设计** 共 11 类需求，形成可审查、可逐条拍板的落地方案。
> 客户分类（P0，已确认设计）作为底座贯穿始终。
> 本文件仅为**方案**，确认后再进入实现；所有改动默认仅前端 `src/index.html`（通知/全局快捷键/复杂重复涉及 Rust 与权限）。

---

## 0. 选中项总览

| 编号 | 借鉴对象 | 需求 | 落地方向 | 阶段 |
|---|---|---|---|---|
| 1.2 | Todoist | 标签 + 过滤器 | 横向跨项目分类 + 条件视图 | 一 |
| 1.3 | Reminders | 智能列表 | 结构化条件持久视图 | 一 |
| 2.1 | Things/TickTick | 行内加子任务 + 全局快捷键 | 就地加子任务；⌘N 全局唤起 | 二 |
| 2.2 | Todoist/Things | 自然语言日期解析 | ⌘K / 新建框解析"明天/每周一" | 二 |
| 2.3 | TickTick | 输入框智能识别 | 标题识别 "!p1" "@标签" 自动设 | 二 |
| 4.1 | TickTick/Things | 跟进类型标签 | 电话/邮件/会议/已回复 | 四 |
| 4.2 | OmniFocus | 周回顾模式 | project.nextReview + 待回顾区 | 四 |
| 4.3 | Things | 完成历史 Logbook | 已完成任务回溯时间线 | 四 |
| 5 | 系统原生 | 本地通知 + 复杂重复 | 截止提醒；每工作日/每N周一 | 五 |
| 8 | macOS HIG | 设计打磨 | 完成动效/空状态/对比度/克制 hover | 六 |
| 视图 | TickTick/OmniFocus | 日历/预测今日/时间线/四象限 | 新增视图层 | 三 |

---

## 1. 总体数据模型（扩展后）

现有 `db` 结构扩展如下（均为本地 JSON，无云端）：

```js
db = {
  clients:   [{ id, name, color }],            // 新增：客户（项目上层分组）
  projects:  [{ id, name, color, clientId }],  // clientId: 归属客户，""=内部
  tags:      [{ id, name, color }],            // 新增：标签（横向跨项目）
  smartLists:[{ id, name, query }],           // 新增：智能列表（持久条件视图）
  tasks:     [{ id, projectId, title, pri, due, repeat,
                tags:[tagId],                  // 新增：任务挂标签
                logs:[{ id, at, type, text, editedAt }], // type 新增
                done, doneAt,                  // doneAt 新增（Logbook 用）
                subtasks:[{ id, title, done }] }],
  projects 额外: nextReview                    // 新增：周回顾用，到期日
}
```

所有新增字段在加载老数据时做兼容补默认（`clients:[]`、`tags:[]`、`smartLists:[]`、`task.tags:[]`、`log.type:""`、`doneAt:0`），不破坏现有数据。

---

## 2. 阶段一：归类维度（客户 + 标签 + 智能列表）

### 2.1 客户分类收口（P0，已确认）
- **数据**：`db.clients=[{id,name,color}]`；`projects[i].clientId`。
- **侧边栏**：新增「客户」分组，列出客户项（色点 + 名称 + 任务计数），hover 出铅笔可重命名/改色；底部「＋ 新建客户」。
- **项目编辑弹窗**：名称/颜色下加「所属客户」下拉（未归类 + 所有客户）。
- **筛选**：点客户 → `ui.cid` 筛选其下所有项目任务；与 `ui.pid` 互斥。
- **报告聚合**：总览/报告增加「按客户」分组（完成率/逾期/任务数）。

### 2.2 标签系统（1.2）
- **数据**：`db.tags=[{id,name,color}]`；`task.tags=[tagId]`。
- **入口**：
  - 任务编辑弹窗加「标签」多选 chips；
  - 侧边栏「标签」分组，点标签即筛选；
  - 支持快速新建标签（输入即建）。
- **卡片展示**：任务卡标题旁显示所挂标签色点/小字。
- **过滤器**：标签 + 客户 + 项目 + 优先级 + 日期 组合筛选（`filtered()` 扩展支持 `ui.tags`）。

### 2.3 智能列表（1.3）
- **数据**：`db.smartLists=[{id,name,query}]`，`query` 为结构化条件对象：
  ```js
  { project:"pid|all", client:"cid|all", tags:[tagId], pri:["P0","P1"],
    dueFrom, dueTo, status:"open|done|all" }
  ```
- **入口**：侧边栏「智能列表」区，点开应用条件渲染列表视图；「＋ 新建智能列表」弹出条件编辑器（复用现有分段/下拉控件）。
- 与 2.2 过滤器共用同一套 `filtered()` 扩展，不复写逻辑。

---

## 3. 阶段二：录入与解析（2.1 / 2.2 / 2.3）

### 3.1 行内加子任务（2.1 · P1）
- 子任务区底部常驻一行「＋ 添加子任务」，点击/聚焦即变输入框，回车即建（仿现有 quickLog 就地展开）。
- 无需进弹窗，与勾选/删除/编辑风格一致。

### 3.2 全局快捷键（2.1 · P2，涉及 Rust）
- Tauri `global-shortcut` feature 注册 `CmdOrCtrl+N` 全局唤起「新建任务」（即使 app 在后台）。
- 与现有 ⌘K（命令面板）分工：⌘N 直接开新建、⌘K 用于跳转/快速建。
- 需在 `main.rs` 加 feature + `capabilities` ACL；设置里可改（复用现有快捷键设置框架）。

### 3.3 自然语言日期解析（2.2 · P1）
- 轻量纯前端解析器（正则 + 词典，无需大模型）：支持
  - 相对："今天/明天/后天/3天后/下周X"
  - 绝对："8月14日"/"14号"
  - 重复："每天/每周一/每月1号"
- 接入 ⌘K 输入框与新建任务框：识别到日期词即自动填 `due`，并把识别出的词从标题剔除。
- 解析失败则保持原样，不阻断输入。

### 3.4 输入框智能识别（2.3 · P2，复用 3.3）
- 标题里识别：`!p1`/`!p2` 设优先级；`@标签名` 自动挂标签（不存在则建）；日期词交给 3.3。
- 与 3.3 共用一个解析函数 `parseTitle(raw)` → `{title, pri, tags, due, repeat}`。

---

## 4. 阶段三：视图设计（第3节）

新增视图入口：在「今日 / 任务 / 总览 / 报告」旁或内部用分段控件切换；日历/预测今日为新增独立视图。

### 4.1 日历视图（P1）
- 月/周网格，任务按 `due` 落格；逾期标红、今日高亮。
- 点击格子 → 该日任务列表；点击任务 → 打开编辑。
- 顶部分段切「月 / 周」。

### 4.2 预测 / 今日视图（P1）
- 「今日」tab 改为左 mini 月历 + 右当日任务（OmniFocus Forecast 风格），一眼看到当天与近期负载，统筹日程。
- 保留现有今日列表逻辑，仅在外层加左栏 mini 日历（可折叠）。

### 4.3 时间线（P2）
- 轻量甘特：按 `due` / 估时显示任务条，可拖拽调期。
- 独立视图，数据来自 `tasks`，不改变存储结构。

### 4.4 四象限（P2）
- 按「重要 / 紧急」分四区：
  - 紧急 ≈ 有近期 `due`；重要 ≈ 高优先级（P0/P1）或用户标「重要」。
  - **需决策**：是否新增任务字段 `important`（布尔）？还是直接复用优先级近似重要。建议新增 `important` 开关（成本低，且让四象限语义清晰）。
- 拖拽任务在四区间移动即改优先级/重要度。

---

## 5. 阶段四：跟进 / 回顾 / 日志（4.1 / 4.2 / 4.3）

### 5.1 跟进类型标签（4.1 · P1）
- `log.type` ∈ {电话, 邮件, 会议, 已回复, 其他}，记录时可选（卡片浮卡与弹窗都加类型选择）。
- 卡片最新跟进浮卡显示类型小图标；报告/总览按类型聚合（如「本周 5 通电话」）。

### 5.2 周回顾模式（4.2 · P1）
- `project.nextReview`：项目编辑弹窗里设「回顾周期 / 下次回顾日」。
- 侧边栏或「今日」加「待回顾」区：到点项目自动进列表；点开逐项目过（复用项目编辑弹窗 + 「已回顾」按钮，更新 `nextReview`）。
- GTD 闭环核心，与客户/项目结构天然契合。

### 5.3 完成历史 Logbook（4.3 · P2）
- 完成任务保留并记 `doneAt`；新增「已完成 / Logbook」视图或在报告里加「完成时间线」。
- 按完成时间回溯，方便写周报。

---

## 6. 阶段五：提醒与时间（5）

### 6.1 本地通知（P1，涉及 Rust + 权限）
- Tauri `notification` feature：任务到期前 N 分钟 / 逾期时系统通知。
- 设置里可开关「到期提醒」与提前量；首次需请求通知权限。
- 后台运行由现有托盘保活支撑（⌘Q 才退）。

### 6.2 复杂重复（P2）
- 扩展现有 `repeat`：支持「每工作日」「每 N 个周一」「每 X 天」等规则（现有 spawnRepeat 逻辑增强，不改存储主键）。

---

## 7. 阶段六：macOS 设计打磨（8）

- **完成动效**：勾选时勾选框缩放 + 任务行淡出/划线微动画（Spring 缓动）。
- **空状态插画**：无任务 / 搜索无结果 / 空客户 等统一淡色收件盘风格插画（复用现有空状态插槽）。
- **深浅色对比度**：复核 muted / faint 文本达 WCAG AA。
- **列表行 hover 更克制**：降低现有 hover 底色强度，改用更轻的高亮边，符合 mac 列表行惯例。

---

## 8. 建议实施顺序

1. **客户分类收口（P0）** —— 进行中，先交付。
2. **标签 + 智能列表（阶段一）** —— 归类闭环，与本次客户分类一脉相承。
3. **自然语言日期 + 输入框识别（阶段二）** —— 录入摩擦，性价比高。
4. **视图层（阶段三）** —— 日历 + 预测今日优先，解决「只看到列表看不到负载」。
5. **周回顾 + 跟进类型（阶段四）** —— 闭环层。
6. **本地通知（阶段五）** —— 系统主动提醒。
7. **macOS 打磨（阶段六）** —— 收尾润色。

---

## 9. 待用户拍板的关键决策

1. **四象限「重要」维度**：新增 `important` 布尔字段，还是复用优先级近似？（建议新增）
2. **视图入口形式**：日历/时间线/四象限作为独立 tab，还是在「任务」内用分段切换？（建议独立 tab + 分段）
3. **全局快捷键 ⌘N**：与 ⌘K 分工是否认可（⌘N 直接新建、⌘K 跳转/快建）？
4. **智能列表条件持久化**：是否支持保存为侧边栏常驻视图（建议支持）？
5. **阶段范围**：本轮是否一次性实现全部 6 阶段，还是先交付「客户+标签+智能列表+自然语言日期+日历/预测今日」这条主线，其余后续？

---

> 下一步：你确认上述方案（含第 9 节决策）后，我按阶段实现；每阶段构建签名交付（沿用现有 .app 交付方式，DMG 打包脚本偶发失败暂以 .app 为准）。
