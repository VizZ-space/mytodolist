# 2026-09-11

## 清理工作区构建产物

- 用户要求清理本目录，先扫描后确认再执行（未碰源码/配置/文档/安装包）
- 删除：`my-task-desktop/src-tauri/target/`（4892 MB / 10751 项）、`node_modules/`、`dist/`
- 保留：`src/index.html`、`src-tauri/src/main.rs`、`Cargo.toml`、`tauri.conf.json`、图标、三份 md、`windows-installer/`
- 清理前 `du -sm` = 4936 MB，`target/` 删完后 src-tauri 只剩源码与配置

## 产出：交互简化优化方案

- 新增 `ux-simplification-plan.md`
- 依据：走查 `src/index.html`（4400 行）+ `main.rs`（1852 行）+ 市场调研（Things 3 / Todoist / Linear / TickTick / Apple Reminders / Google Keep）
- 核心结论：**问题不是流程长（加任务仅 3 步、无必填），而是同屏控件过多与信息架构混乱**
- 实测的复杂度热点（后续改造要盯住这些）：
  - 一级导航 10 项（`tabs` L1627），且 `renderTabbar()` L1687 重复渲染同一份
  - 今天视图同屏 30+ 可点控件；快速添加条默认 6 个控件（`quickAdd` L2169）
  - 任务弹窗 12+ 字段（`openTask` L3252）；项目弹窗 11 字段（`openProject` L3398）
  - 顶部「新建」实际调 `openPalette()`（L3901）→ 点新建得到搜索框
  - 首启 `load()` L1483 直接灌 `demo()` 示例数据，用户分不清示例与真实数据
  - 死入口：`viewTimeline` L2410 孤儿视图、`newSmart` L3782 无触发按钮、`aiBadge` L3040 空操作
- 方案结构：4 个改造包（A 第一屏减负 / B 导航收敛 / C 渐进披露 / D 首次体验）

## 方案修订 v1.1（用户反馈）

- 用户明确：**「笔记」是高频使用的模块，后续要作为个人知识库**
- 因此撤销原方案中「把笔记收进更多视图折叠区」的判断
- 一级导航调整为 5 项：今天 / 日历 / **笔记** / 任务 / 项目
- 新增**改造包 E · 笔记模块升级为个人知识库**（E1 升一级 / E2 工具栏 18→6 / E3 加多标签 / E4 搜索高亮 / E5 双链+反链 / E6 模板+自动归类 / E7 任务闭环）
- 走查确认的笔记现状（比预想好，不要推翻重做）：
  - `viewNotes()` L1949 **已是三栏工作区**：分类栏 `nb-fav` | 列表 `nb-list` | 编辑器 `nb-edit`
  - `noteFiltered()` L1898 的搜索**已能匹配正文**（`title + md`），`noteSnip` L1911 只取开头 80 字
  - 工具栏 L1960–1982 平铺 18 个按钮，其中 H1–H4 占 4 位、B/I/U/S 占 4 位 → 可合并
  - `notes` 表字段：id/title/cat/md/created_at/updated_at/pinned/sort_idx（main.rs L1442）；`note_cats` 为**单层**分类（L1447）
  - 缺口：无多标签、无笔记互链、无反链；任务→笔记为单向关联（`task.note_id`）
- 改动面修正：E3 需给 `notes` 表加 `tags` 列（走 `ensure_col` 兼容追加），**不再宣称"全部纯前端"**

## 方案修订 v1.2（用户反馈）

- 用户明确：**操作习惯要符合大部分人的习惯** → 撤销 v1.1 中所有"高阶用户优先"的设计
- 新增 §3.5《交互通道规范》作为硬约束：**双通道模型**（主通道=可见的按钮/菜单/图标，必须有；加速通道=快捷键/内联语法/hover，可选）+ **11 项大众习惯对照表** + 硬规则「每处快捷键/hover/语法都要有可见等价入口」
- 八条原则：原第 6 条「操作藏在 hover / 右键里」改写为「常驻精简 + `⋯` 溢出菜单」；新增原则 7「可见通道优先 Mouse-First」、原则 8「沿用大众熟悉的既有习惯」
- 逐条翻转的 v1.1 判断：
  - A1 快速添加条：**不用内联语法替代可见控件** → 保留可见的 📅/⚑ 图标按钮，只删掉客户下拉（6 宽控件 → 2）
  - A2 任务卡：v1.1 说"改成 hover 才显示"是**方向反了** → 改为常驻「勾选框 + 标题 + ⋯」三件套
  - A3 右栏：加可见「详情」图标按钮
  - B1 导航：**不隐藏入口**，"收进折叠区默认收起"改为"5 常驻 + 5 降权，全部默认可见"
  - C1 新建：v1.1 的"捕获浮层"改为**就地新增一行**（滴答清单 / 微软 To Do 做法）
  - C4/E2 笔记工具栏：**不用 Notion 式选区浮层**，改为 Word 式常驻图标 + 分隔线分组
  - E5/E6：可视入口（「引用笔记」按钮 / 模板选择器）为主，`[[ ]]` 与 `#分类` 降为加速
  - D3 引导：改为带文字 + 指向可见按钮，不做纯"做中学"
- **新发现一处真 bug**（顺手要修）：`src/index.html` L4343–L4344 在 `var meta=...`（L4345）赋值前就使用 `meta`，因 var 提升不报错但恒为 `undefined` → **`⌘/Ctrl+1–8` 切视图、`⌘/Ctrl+\` 开关右栏实际完全不生效**
- 另一处不一致：`.task .acts`（L280）宽屏**常驻可见**，而 `.pcard .h .iact{opacity:0}`（L286）**悬停才显** → 后者是隐形入口，统一为常驻 `⋯`
- 代码里多处注释写着「macOS 操作习惯」（L835「克制 hover」、L4352「双击编辑 + 右键菜单」）—— 范式参照系选错是系统性问题

## 推送代码到 GitHub

- 用户要求推送到 `https://github.com/VizZ-space/mytodolist`（仓库已存在，空仓库，`main` 分支）
- 本目录此前**无 git 仓库**，本次从零初始化
- 配置：仓库级身份 `VizZ` / `221272360+VizZ-space@users.noreply.github.com`（用户选择 noreply 而非真实 Gmail，隐私考虑）；新增顶层 `.gitignore`
- `.gitignore` 排除：`node_modules/`、`dist/`、`src-tauri/target/`、`src-tauri/gen/schemas/`、`windows-installer/*.exe|*.msi`
- 首次提交 `5aeca35`，73 个文件；已推送到 `origin/main`，本地 HEAD 与远程 SHA 一致
- 踩坑记录已写入 `MEMORY.md`「本机环境注意」：git -C 需用原生路径；本机 fetch 不落盘 `refs/remotes`，需手动写引用文件

## 代码实施：第 0 步 + 改造包 A + 改造包 B

- 用户「帮我进行修改」→ 按方案动 `src/index.html` 单文件（+101 / −38 行）
- **第 0 步**：`var meta` 提到使用点之前，修掉 `Ctrl/⌘+1–9` 与 `Ctrl+\` 恒失效的 bug
- **A1**：placeholder 42 字 → 6 字；语法说明降级为 `title` 悬停提示；控件压成单行
- **A2**：任务卡/项目卡的 3 个图标 → 1 个常驻 `⋯`；移除项目卡 `opacity:0` 悬停隐藏
  - 顺带发现并修掉一个**自己引入的回归**：原右键菜单靠"在卡里找同名按钮再 click"执行动作，删掉卡内按钮后菜单会变哑 → 新增 `runSyntheticAct(act,id,root)`，改成按动作直接走主派发器
- **A3**：新增 `IS_MAC` / `MODKEY`，快捷键提示按平台渲染（Windows 显示 `Ctrl+`）
- **B1/B2/B3**：抽出 `NAV_COMMON` / `NAV_MORE` / `NAV_ALL` / `NAV_KEYS` 单一数据源；侧栏分「常用 / 更多视图」，后者 `.minor` 降权但仍默认可见；修掉 `renderMain` 缺「项目」的标题 bug
- **B4**：`boardTasks()` 改为以 `filtered()` 为基础，消除两套筛选语义

### 验证方法（可复用）

- 构建：`node node_modules/vite/bin/vite.js build`（不要用 npm，本机 PATH 被重置）
- **冒烟测试**：零依赖 CDP 方案 —— Node 起本地静态服务 + `chrome --headless=new --remote-debugging-port` + Node 22 内置 `WebSocket` 直连 CDP，注入断言并收集 `Runtime.exceptionThrown` + `Log.entryAdded`
  - 脚本已固化到 `my-task-desktop/.smoke/smoke.mjs`（已 gitignore），见 `MEMORY.md`「本地验证手段」
  - 注意：模块作用域私有，只能断言 DOM，不能读 `ui` 等变量
  - 键盘类改动用 `document.dispatchEvent(new KeyboardEvent('keydown',{key,ctrlKey:true}))` 验证

### 关键偏差

- **A1「客户由项目推导」做不到**：`projects` 表无 `client_id`，客户与项目是平级实体（已写入 `MEMORY.md`）→ 两个选择器均保留
- 详见 `ux-simplification-plan.md` 第 9 节《实施记录》

## 代码实施：改造包 C + D + E（接续上文）

- 用户「继续，直到刚才的修改意见全部完成」→ 按 v1.3 方案落地 C/D/E 全部内容，动 `src/index.html` + `src-tauri/src/main.rs`
- **C（渐进披露）**：C1 顶部「新建」改为就地聚焦 `#qt`（不再弹命令面板，`⌘K` 降为加速通道）；C2 任务弹窗 12+ 字段 → 默认 4 项 + `details#taskMore「更多选项 ▾」`；C3 项目弹窗同样分层 + `#projMore`；C5 删掉孤儿视图 `viewTimeline` 与空操作 `updateAiBadge`，补上「智能列表 · 新建」入口
- **D（首次体验）**：D1 `load()` 改为 `emptyData()`（不再自动灌示例数据）+ 空态「载入示例数据」按钮 + 载入后「示例数据」横幅与一键清空；D2 统一空状态组件 `emptyState()`（回答"放什么/为什么有用/现在能做什么"）；D3 三步上手引导条（`localStorage.onboardDone` 可跳过）
- **E（笔记 → 知识库）**：E2 工具栏 18 个平铺按钮 → 8 常驻 + `details#ntMore「更多 ▾」`（H1–H4 合并为标题级别下拉）；E3 笔记多标签（复用任务标签体系 + 左侧「标签」分区筛选）；E4 搜索命中高亮 `<mark class="nhit">` + 「N 处匹配」；E5 双链 `[[标题]]` + 反链面板 + 可见入口「引用笔记」；E6 4 个模板 + `#分类` 回车自动归类；E7 任务卡笔记徽标显示笔记标题
- **A1 遗留项落地**：给 `projects` 加 `client_id TEXT`（`CREATE TABLE` + `ensure_col`），`proj_sig` / `read_full` / upsert 三处扩列 → 快速添加行删掉客户下拉，改为 `.qcli` 推导回显；任务详情保留「手动指定」覆盖

### 本轮修掉的 3 个真 bug

1. **`insertWikiLink` 无选区时丢链接**：原实现先写 `ed.innerHTML`，随后又被 `ed.innerHTML=md2html(nn.md)`（旧 md）覆盖 → 「引用笔记」点了没反应。改为：光标在编辑器内就地插入，否则安全追加到末尾；**不用 `deleteContents()` 动编辑器之外的选区、不整段重写已有内容**；光标落点改到链接之后
2. **`notes.tags`（E3）Rust 侧根本没落库 → 真实数据丢失**：`notes` 表读（两处）/写/`note_sig`/`CREATE TABLE` 全都不含 `tags`，前端挂的标签一存一读就没了。已全部补上（存 JSON 数组字符串，与 `tasks.tags` 同格式）。**教训写入 `MEMORY.md`：前端加字段必须同步检查 Rust 四处**
3. **E2 工具栏注释写「6 个常驻」实际 8 个**，已更正

### 验证结果

- `vite build` 通过；**冒烟测试 26 项断言全绿、0 条 JS 运行时错误**（`Runtime.exceptionThrown` + `Log.entryAdded` 双通道）
- 冒烟测试首轮 5 项失败，全部确认为**测试脚本自身的期望值写错**，不是代码缺陷：
  - C2「默认可见字段」把 `display:none` 的「手动指定客户」块和包着 `<details>` 的外层容器也算进去了 → 加 `offsetParent===null` 过滤后 = 4
  - E2 常驻按钮期望写 7，实际 8（B I U S · 标题级别下拉 · 列表 · 引用段落 · 引用笔记）
  - E4 用只出现在**标签名**里的词搜索 → `noteHitCount` 只统计 `title + md`，命中数本就该是 0 → 改用正文里的词
  - E5 两个用例的状态被前序步骤污染（未真正新建第二篇笔记）→ 改为显式用左侧常驻「＋」新建
- `ux-simplification-plan.md` §9 已补齐 C/D/E 实施记录、偏差更新、验收对照
- **Rust 侧**：`cargo test --no-default-features` **8/8 通过**（新增 `note_tags_roundtrip`、`old_notes_table_upgrade_adds_tags`）
  - 首次编译依赖约 14 分钟；之后增量 26 秒
  - 踩坑：新写的测试里 `let sample = serde_json::json!({...}).to_string()` 得到的是 `String`，再对它 `sample.clone()["notes"]` 会报 ``the type `str` cannot be indexed by `&str` ``。**要么让 `sample` 保持 `Value`、只在传参时 `.to_string()`；要么别去索引它**

## 提交与推送（接续上文）

- 提交 `c0bb367`：`refactor(ui): 交互简化 —— 改造包 C/D/E + 客户由项目推导（含 notes.tags 落库修复）`，6 个文件 / +926 −170
- 已推送 `origin/main`（`92a5dd3..c0bb367`），`git ls-remote` 确认远程 SHA = 本地 HEAD
- **再次踩到本机 git 老坑**：push 后 `.git/refs/remotes/origin/main` 变成**空文件**，`git rev-parse origin/main` 报 `unknown revision`。按既定办法手动 `printf '<sha>\n' > .git/refs/remotes/origin/main` 修复，之后 `git status -sb` 显示 `## main...origin/main` 无分叉
- 跟踪文件数仍是 73（本次只改既有文件，未新增；`.smoke/` 已 gitignore）

